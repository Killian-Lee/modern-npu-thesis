#import "/modern-npu-thesis/template.typ": (
  Assign, IfElseChain, Return, While, algorithm, capfig, capsubfig, captab, multicite, nwpu-thesis, 字号
)

#show: nwpu-thesis.with(
  anonymous: false, // 是否开启盲审模式
  title: "基于深度强化学习的虚拟电商推荐算法研究",
  author: "李御科",
  major: "计算机科学与技术",
  supervisor: ("田大湧", "副教授"),
  submit-date: (year: 2026, month: 6),
  abstract: [
    推荐系统是缓解互联网信息过载问题的重要技术。传统推荐方法通常将推荐任务建模为静态偏好预测或点击率预测问题，较难刻画用户与系统之间的连续交互过程。针对虚拟电商推荐场景中的序列决策问题，本文以 Virtual Taobao 为实验平台，基于原始工作发布的预训练用户生成模型、反馈模型和离开模型，封装了符合 Gymnasium 接口的 VirtualTB-v0 强化学习环境，并将推荐过程建模为连续动作空间下的马尔可夫决策过程。

    在方法设计方面，本文首先明确状态、动作、奖励和终止条件：状态由用户画像、上一轮反馈和当前页码组成，动作表示 27 维连续推荐向量，奖励采用虚拟用户反馈中的点击数。随后，本文构建 Random、MLP-SL 和 LinUCB 三类传统对照方法，引入 PPO-SB3 和 DDPG-SB3 作为标准深度强化学习对照，并进一步面向连续动作推荐任务的状态尺度、动作范围和短会话反馈特点，设计 PPO-Rec 和 DDPG-Rec 两种推荐强化学习策略。

    实验在 seed=1、2、3 三个随机种子下进行。结果表明，传统基线方法在该连续动作推荐环境中效果有限，PPO-SB3 和 DDPG-SB3 能够通过环境交互获得一定提升，但平均 CTR 仍低于面向推荐任务改进后的策略。PPO-Rec 的最终 CTR 为 0.8670±0.0529，DDPG-Rec 的最终 CTR 为 0.8958±0.0064，在当前实验设置下高于传统基线和标准 SB3 对照。实验还表明，动作范数可以辅助分析连续动作策略行为，但不能单独作为推荐效果指标。本文结果说明，在 VirtualTB-v0 仿真环境中，结合连续动作推荐任务特点的强化学习训练与策略实现改进能够改善推荐策略表现。
  ],
  keywords: ("深度强化学习", "推荐系统", "Virtual Taobao"),
  abstract-en: [
    Recommender systems are important techniques for alleviating information overload. Traditional recommendation methods usually formulate recommendation as static preference prediction or click-through rate prediction, which makes it difficult to describe the sequential interaction between users and recommender systems. For the virtual e-commerce recommendation scenario, this thesis uses Virtual Taobao as the experimental platform. Based on the pretrained user generation, feedback, and leave models released by the original Virtual-Taobao work, this thesis wraps them into a Gymnasium-compatible VirtualTB-v0 reinforcement learning environment and formulates recommendation as a Markov decision process with a continuous action space.

    In the proposed experimental framework, the state consists of the user profile, previous feedback, and page index; the action is a 27-dimensional continuous recommendation vector; and the reward is defined as the click feedback returned by the virtual user model. This thesis implements Random, MLP-SL, and LinUCB as traditional baselines, introduces PPO-SB3 and DDPG-SB3 as standard deep reinforcement learning references, and further designs PPO-Rec and DDPG-Rec as recommendation-oriented reinforcement learning policies for continuous-action recommendation tasks.

    Experiments are conducted with three random seeds. The results show that traditional baselines have limited performance in this continuous-action recommendation environment. PPO-SB3 and DDPG-SB3 can improve performance through environment interaction, but their average CTR remains lower than that of the recommendation-oriented policies. PPO-Rec achieves a final CTR of 0.8670±0.0529, and DDPG-Rec achieves a final CTR of 0.8958±0.0064, both outperforming the traditional baselines and SB3 references under the current experimental setting. The analysis also shows that action norm is useful for understanding continuous-action policy behavior, but it should not be treated as a direct recommendation metric. These results indicate that reinforcement learning policies designed for continuous-action recommendation can improve recommendation performance in VirtualTB-v0. The conclusions of this thesis are limited to the fixed virtual environment and should not be directly generalized to real online e-commerce systems.

  ],
  keywords-en: ("Deep Reinforcement Learning", "Recommender Systems", "Virtual Taobao"),
  appendix: [
    == 实验配置补充说明

    本文主实验均围绕 VirtualTB-v0 虚拟电商推荐环境展开。为保证不同方法之间具有可比性，实验统一采用 seed=1、2、3 三个随机种子进行重复评估，并在最终结果中报告均值和标准差。传统基线方法 Random、MLP-SL 和 LinUCB 主要报告最终评估指标；PPO-SB3、DDPG-SB3、PPO-Rec 和 DDPG-Rec 除最终评估外，还记录训练过程中的周期评估结果，用于分析收敛趋势和学习速度。

    主实验中，PPO-SB3、DDPG-SB3 和 PPO-Rec 的训练预算均为 100 万交互步，DDPG-Rec 的训练预算为 30 万交互步。DDPG-Rec 训练步数较少，是因为其在前期实验中表现出较高样本效率。论文正文在比较学习速度和最终效果时已对这一差异进行说明，避免将不同预算下的结果作无条件比较。

    主要评估指标包括平均点击率、平均累计奖励、平均会话长度和动作范数。其中，平均点击率和平均累计奖励用于衡量推荐策略效果；动作范数仅用于辅助分析连续动作策略的输出行为，不作为推荐效果的直接指标。所有最终结论均限定在 VirtualTB-v0 固定模拟环境内。

    == 结果文件说明

    最终实验结果整理于 `results/final_rebuild_analysis/`。其中，`comparison/summary_mean_std.csv` 保存各方法在三个随机种子下的最终均值和标准差；`comparison/eval_history_merged.csv` 保存深度强化学习方法训练过程中的周期评估记录；`comparison/thresholds_and_peaks.csv` 保存不同方法达到若干 CTR 阈值的训练步数和历史峰值。

    论文正文中使用的主要图表来自 `results/final_rebuild_analysis/comparison/figures/`，并已同步到论文模板目录下的 `figures/final_rebuild/`。其中，最终 CTR 柱状图用于比较整体性能，seed 散点图用于观察稳定性，训练 CTR 曲线用于分析收敛过程，动作范数曲线用于辅助解释连续动作策略行为，阈值步数图用于观察不同强化学习方法的学习速度。

    == 复现实验流程说明

    复现实验时，应先确认 VirtualTB-v0 环境能够正常加载原始 Virtual-Taobao 发布的预训练用户生成模型、反馈模型和离开模型。随后分别运行传统基线、标准深度强化学习对照和 PPO-Rec、DDPG-Rec 方法，并在每个随机种子下保存训练参数、周期评估记录和最终评估结果。所有实验完成后，可使用统一的结果分析脚本读取各方法的 `metrics_summary.json` 和 `eval_metrics_history.csv`，生成汇总表、训练曲线和论文图表。

    由于不同算法的训练机制不同，复现实验时应特别注意以下事项：第一，LinUCB 是基于离线样本和固定动作原型的上下文 Bandit 方法，不具有与 PPO、DDPG 相同意义上的训练曲线；第二，MLP-SL 的训练损失下降不必然代表环境 CTR 提升，最终仍需放回 VirtualTB-v0 环境进行 episode 评估；第三，深度强化学习方法的周期评估结果可能与最终 best model 的正式评估略有差异，因此论文中的最终比较应以统一整理后的正式评估结果为准。
  ],
  acknowledgement: [
    本论文的完成离不开老师、同学和家人的帮助与支持。

    首先，衷心感谢田老师在毕业设计选题、研究思路、实验设计和论文写作过程中给予的指导。导师严谨的治学态度和耐心细致的修改意见，使我能够逐步明确课题边界，避免将实验结果作过度解释，并在论文写作中更加重视论据、数据和结论之间的一致性。

    其次，感谢学院各位老师在本科阶段课程学习和科研训练中给予的帮助。计算机专业基础课程、机器学习相关课程以及实践训练，为本文开展强化学习推荐算法研究提供了必要的知识基础。

    同时，感谢师兄在实验运行、结果讨论和论文修改过程中给予的建议与鼓励。毕业设计周期较长，实验结果也曾多次调整，在交流与讨论中我逐渐加深了对推荐系统、强化学习和实验复现的理解。

    最后，感谢家人在学习和生活上的支持。正是他们长期以来的理解与鼓励，使我能够较为专注地完成本科阶段的最后一项综合性学习任务。谨向所有关心和帮助过我的老师、同学、朋友和家人表示诚挚的感谢。
  ],
  design_summary: [
    本次毕业设计围绕“基于深度强化学习的虚拟电商推荐算法研究”展开，主要完成了虚拟电商推荐环境建模、传统推荐基线构建、深度强化学习策略训练、实验结果分析和论文撰写等工作。通过该课题，我对推荐系统和强化学习从理论到实验的完整流程有了更加系统的认识。

    在课题前期，我首先梳理了推荐系统、强化学习和 Virtual Taobao 相关资料，明确真实电商推荐系统难以直接在线试错，因此需要借助可交互虚拟环境开展算法研究。在此基础上，本文将原始 Virtual-Taobao 发布的预训练用户生成模型、反馈模型和离开模型组织为 VirtualTB-v0 强化学习环境，并围绕状态、动作、奖励和终止条件建立连续动作推荐任务。

    在算法与实验部分，本文不仅实现了 Random、MLP-SL 和 LinUCB 等传统对照方法，也引入 PPO-SB3 和 DDPG-SB3 作为标准深度强化学习对照，并进一步设计 PPO-Rec 和 DDPG-Rec 两种面向连续动作推荐任务的强化学习策略。通过多随机种子实验和统一指标分析，本文比较了不同方法在 CTR、累计奖励、动作范数和稳定性方面的差异。实验结果表明，强化学习方法能够在虚拟电商环境中学习有效推荐策略，而面向推荐任务特点的训练设置和策略实现对最终效果具有明显影响。

    在完成过程中，我也认识到实验结果解释必须保持谨慎。VirtualTB-v0 虽然来源于原始 Virtual-Taobao 工作中的预训练虚拟用户模型，但仍然是固定模拟环境，不能简单等同于真实线上电商系统。因此，本文在写作中明确限定结论适用范围，并将较高 CTR 解释为固定模拟环境中的策略表现，而不是现实平台中的直接效果。

    通过本次毕业设计，我提升了文献阅读、实验设计、结果分析和论文写作能力，也进一步理解了工程实现与学术表达之间的差异。后续若继续深入该方向，可以从更丰富的用户状态表示、多目标奖励设计、跨环境泛化验证和离线强化学习等方面展开研究，使强化学习推荐策略更接近真实推荐系统中的应用需求。
  ],
)

= 绪论
== 课题研究背景及意义

随着互联网技术和电子商务平台的快速发展，用户每天接触到的商品、新闻、视频、广告等信息规模不断扩大。面对海量信息，用户往往难以在有限时间内主动筛选出符合自身兴趣和需求的内容。推荐系统通过分析用户历史行为、兴趣偏好、上下文信息以及物品特征，能够为用户提供个性化的信息过滤服务，已广泛应用于电子商务、短视频、新闻资讯、在线音乐和社交媒体等场景。推荐系统不仅能够提升用户获取信息的效率，也能够提高平台的点击率、转化率和用户留存率，因此具有重要的理论研究价值和实际应用意义。

传统推荐算法主要包括协同过滤、矩阵分解、基于内容的推荐以及近年来广泛应用的深度学习推荐模型#multicite("Adomavicius2005RecommenderSurvey", "Koren2009MatrixFactorization", "He2017NCF")。这类方法通常将推荐任务建模为评分预测、点击率预测或排序问题，关注的是在当前状态下预测用户对某一物品的即时反馈。然而，在真实推荐场景中，用户与系统之间往往不是一次性交互，而是一个连续、多轮、动态变化的过程。例如，在电商平台中，用户可能连续浏览多个页面，系统每一次推荐结果都会影响用户下一步的点击、购买、翻页或离开行为。因此，推荐系统不仅需要优化当前页面的点击率，还应考虑长期用户满意度、累计收益以及用户兴趣随时间变化的问题。

强化学习是一类面向序列决策问题的机器学习方法，其核心思想是智能体通过与环境交互，根据环境反馈的奖励信号不断优化策略，从而最大化长期累计回报。与传统监督学习方法相比，强化学习能够更加自然地处理“状态—动作—奖励”的连续决策过程，适合解决推荐系统中的延迟反馈、探索与利用平衡、长期收益优化等问题。因此，将深度强化学习引入推荐系统，已经成为近年来推荐算法研究中的重要方向之一。

然而，强化学习方法在真实推荐系统中的应用仍然面临较大困难。一方面，深度强化学习通常需要大量交互样本，如果直接在真实电商平台上进行在线试错，可能会带来较高的用户体验风险和商业成本；另一方面，真实推荐系统复杂度较高，用户行为具有随机性和动态性，研究者难以在真实环境中进行可控、可复现的算法实验。为解决这一问题，Shi 等提出了 Virtual Taobao 平台，通过历史用户行为数据构建虚拟电商环境，使强化学习算法能够在离线模拟环境中进行训练和评估@Shi2019VirtualTaobao。Virtual Taobao 的提出为强化学习推荐算法研究提供了一个具有代表性的实验平台，也为推荐系统中的序列决策建模提供了新的研究思路。

基于上述背景，本文以 Virtual Taobao 作为实验平台，围绕“基于深度强化学习的推荐算法研究”展开工作。本文尝试将推荐过程建模为马尔可夫决策过程，比较不同深度强化学习算法在连续动作推荐环境中的表现，并引入传统推荐基线方法进行对比分析。该研究的主要意义在于，在固定虚拟环境中建立统一对比框架，用于观察不同推荐策略在连续动作推荐任务中的表现差异。

== 国内外研究现状

推荐系统的研究最早主要围绕协同过滤和基于内容的推荐方法展开@Adomavicius2005RecommenderSurvey。协同过滤方法通过挖掘用户与物品之间的历史交互关系进行推荐，具有较好的实用性，但在数据稀疏、冷启动和用户兴趣动态变化等场景下存在一定局限。随着深度学习的发展，研究者开始将神经网络引入推荐系统，用于学习用户和物品的低维表示，建模复杂的非线性交互关系，从而提升点击率预测和排序效果#multicite("Covington2016YouTube", "He2017NCF")。

尽管深度学习方法在推荐任务中取得了较好效果，但多数方法仍主要关注单步预测问题，即根据当前用户状态预测用户是否点击或购买某个物品。这类方法通常假设样本之间相互独立，难以充分刻画用户与推荐系统之间的连续交互过程。事实上，推荐系统中的每一次推荐都会改变用户后续的行为路径，当前推荐结果可能影响用户继续浏览、购买或离开平台。因此，越来越多研究开始将推荐任务视为序列决策问题，并引入强化学习方法进行建模。

在强化学习基础算法方面，深度 Q 网络（Deep Q-Network，DQN）将深度神经网络与 Q-learning 结合，在高维状态空间下取得了突破性进展@Mnih2015DQN。随后，策略梯度和 Actor-Critic 类方法被广泛研究，其中 TRPO 通过限制策略更新幅度提高训练稳定性@Schulman2015TRPO，PPO 在此基础上采用裁剪目标函数，兼顾实现简洁性和训练稳定性@Schulman2017PPO。对于连续动作空间问题，DDPG 将确定性策略梯度与深度神经网络结合，能够直接输出连续控制动作@Lillicrap2015DDPG；SAC 则引入最大熵强化学习思想，在优化累计回报的同时鼓励策略保持探索性@Haarnoja2018SAC。这些算法为深度强化学习在推荐系统中的应用提供了重要的方法基础。结合本文的 VirtualTB-v0 环境特点，后续主实验重点选择 PPO 和 DDPG：前者代表较稳定的 on-policy 策略优化思路，后者代表样本利用率较高的 off-policy 连续动作控制思路。

在强化学习推荐系统研究方面，国外学者主要从会话推荐、新闻推荐、广告推荐和电商推荐等场景开展探索。相比传统推荐方法，强化学习推荐方法能够根据用户反馈不断调整推荐策略，并以长期累计收益作为优化目标。相关综述研究指出，强化学习推荐系统通常需要解决状态表示、动作空间设计、奖励函数构造、环境建模以及离线评估等关键问题@Afsar2022RLRSurvey。其中，如何构建可靠的交互环境是强化学习推荐研究中的核心难点之一。由于真实平台在线试错成本较高，基于模拟环境进行离线训练和验证成为一种重要研究思路。与此同时，基于模拟器的研究也需要注意离线评估偏差、模拟器偏差、奖励设计偏差和策略过度适应等问题，避免将虚拟环境中的结果直接等同于真实线上效果。

国内在推荐系统和强化学习应用方面的研究起步较早，尤其在电子商务、信息流推荐、广告投放和搜索排序等产业场景中积累了较丰富的实践经验。随着阿里巴巴、腾讯、字节跳动、百度等大型互联网平台的发展，推荐系统逐渐从传统的点击率预估和排序模型，向多目标优化、长期价值建模和在线决策方向演进。在这一过程中，强化学习因其能够处理连续交互和长期收益优化问题，逐渐受到国内学术界和工业界的关注。

在电商推荐和搜索场景中，国内研究者较早尝试将强化学习引入排序和推荐任务。Hu 等针对电商搜索引擎提出强化学习排序方法，将用户在搜索过程中的连续反馈建模为序列决策问题，使排序策略不仅关注当前页面效果，也考虑后续用户行为和长期收益@Hu2018RLRank。Chen 等则关注动态推荐环境中的强化学习稳定性问题，研究如何在用户兴趣和环境变化的情况下提升推荐策略的鲁棒性@Chen2018StabilizingRL。这些研究表明，强化学习方法在电商搜索和推荐场景中具有较强的应用潜力，但同时也面临训练样本需求大、在线实验风险高和策略稳定性不足等问题。

Virtual Taobao 是国内学术界与工业界结合的代表性成果之一。该工作由南京大学和阿里巴巴相关研究者提出，目标是在不直接影响真实用户体验的情况下，为强化学习推荐算法提供可训练、可评估的虚拟环境@Shi2019VirtualTaobao。与一般的公开推荐数据集不同，Virtual Taobao 不仅提供离线交互数据的建模思路，还试图构建用户生成、交互模拟和策略训练的完整流程。该研究对于国内强化学习推荐系统的发展具有重要意义，因为它从工程实践角度回应了强化学习在真实推荐系统中难以直接在线试错的问题。

近年来，国内关于强化学习推荐系统的综述和应用研究也逐渐增多。相关研究普遍认为，强化学习推荐方法在处理用户兴趣漂移、长期收益优化、多轮会话推荐等问题上具有优势，但仍然存在环境构建困难、奖励设计复杂、离线评估不充分、策略泛化能力不足和线上安全探索受限等挑战。对于本科阶段的算法研究而言，直接接入真实商业推荐系统并不现实，而基于 Virtual Taobao 这样的虚拟环境开展算法复现、对比和改进，可以在可控条件下研究不同深度强化学习算法的性能差异。

总体来看，国内外已有研究为深度强化学习推荐系统奠定了较好的理论和方法基础，但仍存在以下问题：第一，许多研究更关注单一算法效果，缺乏在统一环境下对多种连续动作强化学习算法的系统比较；第二，部分研究只比较强化学习算法之间的差异，缺少与传统推荐方法或上下文 Bandit 方法的对照，难以充分说明强化学习方法本身的有效性；第三，模拟环境与真实环境之间可能存在差异，强化学习策略可能出现对模拟器过拟合的问题。因此，本文选择 Virtual Taobao 作为实验平台，在统一环境下比较传统推荐基线、上下文 Bandit 方法和多种深度强化学习算法，以期更全面地分析深度强化学习推荐方法的适用性和局限性。

== 论文主要工作内容

首先，本文对推荐系统、强化学习以及深度强化学习推荐方法的相关理论进行梳理，分析传统推荐方法在序列决策和长期收益优化方面的不足，并结合 Virtual Taobao 环境说明将推荐过程建模为马尔可夫决策过程的合理性。在该建模方式下，推荐系统被视为智能体，虚拟用户及其反馈被视为环境，智能体根据当前用户状态选择推荐动作，并根据点击反馈获得奖励。

其次，本文基于原始 Virtual-Taobao 工作发布的预训练用户生成模型、反馈模型和离开模型，构建 VirtualTB-v0 强化学习环境。本文不重新训练这些环境内部模型，也不直接处理真实淘宝原始日志，而是复用原始工作基于真实匿名交互数据训练得到的预训练模型，将其组织为符合 Gymnasium 规范的虚拟电商推荐环境。通过统一状态空间、动作空间、奖励定义和评价指标，不同推荐策略能够在同一环境中进行训练和评估。

再次，本文设计多类推荐策略进行对比。传统方法方面，本文选取 Random、MLP-SL 和 LinUCB，分别表示无学习策略、离线监督学习策略和只优化即时反馈的上下文 Bandit 策略。强化学习方法方面，本文引入 PPO-SB3 和 DDPG-SB3 作为通用深度强化学习对照，并面向连续动作推荐任务的状态尺度、动作范围和短会话反馈特点，设计 PPO-Rec 和 DDPG-Rec 两种推荐强化学习策略。通过上述方法对比，本文能够分析面向推荐任务的策略改进对连续动作推荐训练效果的影响。

最后，本文从点击率、平均累计奖励、用户会话长度、动作范数以及不同随机种子下的稳定性等指标对实验结果进行分析。通过比较不同算法在 VirtualTB-v0 环境中的表现，本文讨论深度强化学习方法在连续动作虚拟推荐任务中的有效性、训练收益和局限性。

本文的整体技术路线如@fig-research-framework 所示。全文首先从推荐系统序列决策建模出发，构建 VirtualTB-v0 虚拟电商推荐环境；随后设计传统基线、标准深度强化学习对照和面向推荐任务特点的强化学习策略；最后通过多随机种子实验分析各类方法在点击率、累计奖励、动作范数和训练稳定性方面的表现。

#capfig(
  image("figures/research_framework.png", width: 95%),
  caption: [本文技术路线图],
  label: <fig-research-framework>,
)

== 论文结构安排

本文共分为五章，各章内容安排如下：

第一章为绪论。本章主要介绍课题的研究背景及意义，分析深度强化学习推荐算法的国内外研究现状，说明本文的主要研究内容，并给出全文结构安排。

第二章为相关理论与技术基础。本章将介绍推荐系统的基本概念、传统推荐算法的基本思想、强化学习的基本理论、深度强化学习中的典型算法，以及 Virtual Taobao 实验平台的基本原理，为后续算法设计和实验分析奠定理论基础。

第三章为基于 VirtualTB-v0 的推荐算法设计与实现。本章将介绍本文使用的虚拟电商环境、预训练环境模型来源、状态空间、动作空间和奖励函数，并对 Random、MLP-SL、LinUCB、PPO-SB3、DDPG-SB3、PPO-Rec 和 DDPG-Rec 等主实验方法进行说明。

第四章为实验结果与分析。本章将在统一实验设置下对不同推荐算法进行训练和评估，比较各算法在点击率、累计奖励、会话长度和稳定性等指标上的表现，并结合实验结果分析深度强化学习方法在 Virtual Taobao 环境中的有效性和局限性。

第五章为总结与展望。本章将总结全文的主要研究工作和实验结论，分析本文研究中存在的不足，并对后续工作进行展望，例如进一步改进状态表示方式、引入更合理的动作约束机制、扩展更多推荐场景以及提高虚拟环境与真实环境之间的一致性等。

= 相关理论与技术基础

== 推荐系统基础

随着互联网平台中信息规模的不断扩大，用户在电子商务、新闻资讯、短视频、音乐、广告等场景中面临严重的信息过载问题。推荐系统作为一种重要的信息过滤技术，能够根据用户的历史行为、兴趣偏好、上下文信息以及物品特征，为用户筛选并推荐其可能感兴趣的内容。推荐系统的目标不仅是提高用户获取信息的效率，也包括提升平台点击率、转化率、用户留存率和长期商业价值。因此，推荐系统已经成为电子商务和互联网服务中的核心技术之一。

从基本形式上看，推荐系统通常包含用户、物品和用户行为三类核心对象。用户表示接受推荐服务的主体，物品表示系统可以推荐的商品、视频、新闻或广告等内容，用户行为则包括点击、浏览、收藏、购买、评分、停留时长等反馈信息。推荐算法的主要任务是根据已有用户行为和物品信息，预测用户对候选物品的偏好程度，并生成个性化推荐结果。已有推荐系统综述通常将传统推荐方法概括为基于内容的推荐、协同过滤推荐和混合推荐三类，并指出推荐系统需要在用户建模、上下文信息利用和推荐灵活性等方面不断扩展@Adomavicius2005RecommenderSurvey。

=== 推荐系统基本概念

推荐系统的本质是根据用户需求和物品特征进行个性化信息匹配。与普通搜索系统不同，搜索系统通常依赖用户主动输入查询词，而推荐系统更多依赖系统对用户兴趣的主动建模。用户可能并不清楚自己具体想要什么，推荐系统则需要根据用户历史行为和当前上下文推断其潜在需求。

如@推荐系统基本流程 所示，推荐系统通常由信息输入、推荐算法建模、候选物品排序、推荐结果展示和用户反馈等环节组成。系统首先收集用户信息、历史行为、上下文信息以及物品信息等数据，并将其作为推荐算法模型的输入；随后，推荐算法对候选物品进行评分或排序，生成面向用户的推荐列表；用户在浏览推荐结果后产生点击、购买、停留时长等反馈信息，这些反馈又可以进一步作为新的历史行为数据，用于后续推荐模型的更新与优化。

#capfig(
  image("figures/推荐系统基本流程图.png", width: 75%),
  caption: [推荐系统的基本流程图],
  label: <推荐系统基本流程>,
)

在典型推荐系统中，用户集合可记为 $U={u_1,u_2,⋯,u_m}$，物品集合可记为 $I={i_1,i_2,⋯,i_n}$。推荐系统的目标是学习一个偏好预测函数，用于估计用户 $u$ 对物品 $i$ 的兴趣程度：$ hat(y)_"ui" = f(u, i) $
其中，$hat(y)_"ui"$ 表示模型预测的用户偏好分数，$f(⋅)$ 表示推荐模型。系统可以根据预测分数对候选物品进行排序，并向用户展示排名靠前的物品。

用户反馈通常可以分为显式反馈和隐式反馈两类。显式反馈包括评分、评论、点赞等用户主动表达的偏好信息；隐式反馈则包括点击、浏览、购买、停留时长、加购等用户行为。与显式反馈相比，隐式反馈在实际平台中更容易获取，但其含义往往更加复杂。例如，用户点击某个商品不一定代表最终购买，用户未点击某个商品也不一定代表不感兴趣。因此，如何从复杂的用户行为中准确建模用户偏好，是推荐系统研究中的重要问题。

从工程流程看，推荐系统通常并不是一次性从全部物品中直接选出最终展示内容，而是分阶段完成。常见流程包括候选召回、排序、重排和展示等步骤。召回阶段从大规模物品库中筛选出较小的候选集合，排序阶段根据用户特征、物品特征和上下文信息对候选物品打分，重排阶段进一步考虑多样性、业务规则或用户体验约束。本文所研究的 VirtualTB-v0 环境并不显式提供真实商品集合或 item_id，而是使用连续动作向量表示推荐策略输出。因此，本文更关注“推荐策略如何在连续交互环境中选择动作”，而不是传统意义上的候选物品召回和排序实现。

=== 传统推荐算法概述

传统推荐算法主要包括基于内容的推荐、协同过滤推荐、矩阵分解方法以及混合推荐方法。

基于内容的推荐方法主要利用物品自身特征进行推荐。例如，在商品推荐场景中，可以根据商品类别、品牌、价格、文本描述等信息计算物品之间的相似度，然后向用户推荐与其历史偏好相似的物品。该方法的优点是具有较好的可解释性，并且对新物品相对友好；但其缺点是容易产生推荐内容同质化问题，即系统倾向于反复推荐与用户历史兴趣高度相似的物品，难以发现用户潜在的新兴趣。

协同过滤是推荐系统中应用广泛的一类方法，其核心思想是利用用户之间或物品之间的相似性进行推荐。基于用户的协同过滤认为，如果两个用户过去喜欢的物品相似，则他们未来可能也会喜欢相似的物品；基于物品的协同过滤则认为，如果多个用户同时喜欢两个物品，则这两个物品具有较高相似性。协同过滤不依赖复杂的物品内容特征，能够发现用户行为中的群体偏好，但在用户行为数据稀疏或新用户、新物品较多时，容易面临数据稀疏和冷启动问题。

矩阵分解方法是协同过滤的重要发展方向。该方法通常将用户—物品交互矩阵分解为用户隐向量和物品隐向量，通过二者的内积预测用户对物品的偏好。已有研究表明，矩阵分解模型能够融合隐式反馈、时间因素和置信度等信息，在推荐任务中具有较好的表现@Koren2009MatrixFactorization。其基本形式可以表示为：
$ hat(r)_"ui"​=p_u^T q_i $
其中，$p_u$ 表示用户 $u$ 的隐向量，$q_i$ 表示物品 $i$ 的隐向量，$hat(r)_"ui"$ 表示用户 u 对物品 i 的预测偏好。矩阵分解方法结构简洁、效果稳定，但通常仍然主要面向静态评分预测或偏好预测问题，难以直接处理用户与推荐系统之间的连续交互过程。

混合推荐方法通过组合多种推荐策略来弥补单一方法的不足。例如，可以将基于内容的推荐与协同过滤结合，也可以将规则方法、知识图谱方法和深度学习模型结合。混合方法可以通过融合不同推荐技术提升推荐质量和鲁棒性@Burke2002Hybrid。在实际工业系统中，推荐流程通常不是单一模型完成，而是包括召回、粗排、精排和重排等多个阶段，并在不同阶段使用不同类型的模型。

近年来，深度学习方法被广泛应用于推荐系统。深度推荐模型能够通过神经网络学习用户与物品之间复杂的非线性交互关系，并融合文本、图像、行为序列、上下文等多源信息。例如，大规模工业推荐系统常使用深度神经网络进行候选生成和排序建模@Covington2016YouTube，部分神经协同过滤方法也使用神经网络替代传统矩阵分解中的内积操作，以更灵活地建模用户和物品之间的交互关系@He2017NCF。

总体而言，传统推荐算法和深度推荐模型在静态偏好建模、点击率预测和排序任务中取得了较好效果，但它们通常仍将推荐问题视为单步预测问题，即根据当前状态预测用户是否会点击或购买某个物品。对于需要连续多轮交互和长期收益优化的场景，仅依赖单步预测可能难以获得全局最优的推荐策略。

需要特别说明的是，标准协同过滤、矩阵分解等方法通常依赖用户编号、物品编号和用户—物品交互矩阵#multicite("Adomavicius2005RecommenderSurvey", "Koren2009MatrixFactorization")，而本文所使用的 VirtualTB-v0 环境提供的是 91 维状态向量、27 维连续动作向量和点击反馈，并没有显式候选商品集合。因此，标准 UserCF、ItemCF 或矩阵分解方法不能直接自然地作为本文主实验方法。若要构造类似协同过滤的补充实验，只能将若干动作原型视为“伪物品”，再基于用户特征分桶构造近似交互矩阵。这类方法更适合称为 Action-Prototype Collaborative Filtering，而不能等同于标准推荐系统中的协同过滤。基于这一原因，本文主实验选择 Random、SL-BC、LinUCB 和深度强化学习方法作为对照。

=== 推荐系统中的序列决策问题

在真实推荐场景中，用户与推荐系统之间的交互往往不是一次性的，而是一个连续动态过程。以电商平台为例，用户进入平台后可能会浏览多个页面，点击若干商品，继续翻页，加入购物车，完成购买，或者直接离开平台。推荐系统在某一时刻展示的内容不仅影响用户当前是否点击，也可能影响用户后续是否继续浏览、是否购买以及是否留在平台中。

因此，推荐系统中的许多任务具有明显的序列决策特征。系统在每一步都需要根据当前用户状态选择推荐动作，用户产生反馈后，系统再根据新的状态继续做出决策。这一过程如@推荐系统序列决策 所示：
#capfig(
  image("figures/推荐系统中的序列决策过程.png", width: 80%),
  caption: [推荐系统中的序列决策过程],
  label: <推荐系统序列决策>,
)

其中，$s_t$ 表示第 $t$ 步的用户状态，$a_t$ 表示推荐系统采取的推荐动作，$r_t$ 表示用户反馈形成的奖励，$s_"t+1"$ 表示下一时刻的用户状态。与传统推荐方法相比，这种建模方式更关注动作对后续状态和长期收益的影响。

传统监督学习推荐方法通常关注即时反馈，例如当前推荐列表的点击率或某个物品的点击概率。然而，在实际系统中，某些能够提高当前点击率的推荐策略，未必能够提升长期用户满意度。例如，过度推荐标题吸引力强但内容质量较低的商品或信息，可能短期内提高点击率，但长期来看可能导致用户体验下降。相反，有些推荐策略虽然短期点击收益不高，但可能更有利于用户持续浏览和长期留存。因此，推荐系统需要在即时收益和长期收益之间进行权衡。

强化学习方法适合处理这类序列决策问题。与传统推荐方法相比，强化学习不仅关注当前动作带来的即时奖励，还通过累计回报刻画策略的长期效果。同时，强化学习能够处理探索与利用问题，即系统既要利用已有知识推荐当前最可能带来收益的内容，也要探索新的推荐动作以发现用户潜在兴趣。基于这一特点，强化学习推荐系统逐渐成为推荐算法研究中的重要方向。

不过，将推荐系统建模为序列决策问题也会带来新的困难。首先，用户状态并不完全可观测，系统只能通过用户画像和历史反馈近似描述用户兴趣；其次，推荐动作可能影响用户后续行为，因此训练策略时需要考虑动作对状态转移的影响；最后，真实线上系统中直接探索存在风险，不成熟策略可能损害用户体验。本文使用 Virtual Taobao 的意义正在于此：它提供了一个可交互的虚拟环境，使算法能够在不影响真实用户的情况下进行训练和评估。

== 强化学习基础

=== 强化学习基本框架

强化学习研究的是智能体在环境中通过试错学习策略的问题。其基本过程可以描述为：在时刻 $t$，智能体观察到环境状态 $s_t$，根据当前策略选择动作 $a_t$，环境接收动作后转移到下一状态 $s_(t+1)$，并向智能体返回奖励 $r_t$。智能体的目标不是仅最大化某一步的即时奖励，而是在长期交互过程中获得尽可能高的累计回报。

与监督学习不同，强化学习中的训练信号通常不是预先给定的标准标签，而是由环境根据智能体动作产生的奖励。智能体需要在交互过程中不断修正策略，同时面对探索与利用、延迟奖励、环境随机性等问题。在推荐系统中，智能体可以对应推荐策略，环境可以对应用户及其反馈行为，动作可以对应推荐内容或推荐向量，奖励则可以由点击、购买、停留或其他反馈构成。

强化学习的基本目标通常可以写为最大化期望折扣回报：

$ J("pi") = E_"pi" [sum_(t=0)^infinity "gamma"^t r_t] $

其中，$"pi"$ 表示策略，$"gamma" in [0, 1]$ 表示折扣因子。当 $"gamma"$ 较大时，算法更加关注长期收益；当 $"gamma"$ 较小时，算法更加关注近期奖励。对于推荐系统而言，折扣回报的思想有助于将多轮用户交互纳入统一优化目标。

在推荐场景中，状态、动作和奖励的含义与传统控制任务略有不同。状态通常不是物理系统的位置或速度，而是用户画像、历史点击、当前会话位置等信息；动作也不一定是离散物品编号，在本文环境中表现为连续推荐向量；奖励则通常来自用户反馈，例如点击数、购买行为或停留时长。理解这些对应关系，有助于将通用强化学习算法合理迁移到推荐系统中。

=== 马尔可夫决策过程

马尔可夫决策过程（Markov Decision Process，MDP）是强化学习中常用的数学建模框架。一个 MDP 通常可以表示为五元组 $(S, A, P, R, "gamma")$，其中 $S$ 表示状态空间，$A$ 表示动作空间，$P(s_(t+1) | s_t, a_t)$ 表示状态转移概率，$R(s_t, a_t)$ 表示奖励函数，$"gamma"$ 表示折扣因子。

马尔可夫性假设认为，下一时刻状态只与当前状态和当前动作有关，而与更早的历史无关。在真实推荐系统中，用户兴趣和历史行为往往较为复杂，很难完全满足严格的马尔可夫性。但在工程建模时，可以将用户画像、历史反馈和上下文信息编码到状态表示中，使当前状态尽可能包含决策所需的信息。

在本文所使用的 VirtualTB-v0 环境中，状态由 91 维向量表示，其中包括用户静态特征、上一轮点击反馈、上一轮反馈索引和当前页面编号；动作是 27 维连续向量；奖励由环境中的用户反馈模型返回，主要对应点击数。通过这种抽象，电商推荐过程可以被建模为一个连续动作空间下的 MDP。

需要注意的是，本文中的 MDP 建模是一种工程近似。真实用户兴趣可能受到更长历史、外部上下文和随机因素影响，单个状态向量无法完全描述所有信息。因此，本文实验结论应理解为在 VirtualTB-v0 固定模拟环境下的策略比较，而不是对真实线上用户行为的完整刻画。

=== 策略、价值函数与动作价值函数

策略用于描述智能体在给定状态下如何选择动作。对于随机策略，策略可表示为 $"pi"(a | s)$，即在状态 $s$ 下选择动作 $a$ 的概率分布；对于确定性策略，策略可表示为 $a = "mu"(s)$，即直接输出一个动作。在连续动作空间中，策略通常由神经网络参数化，网络输入状态向量，输出连续动作向量或动作分布参数。

价值函数用于衡量某一状态或状态—动作对的长期收益。状态价值函数 $V^"pi"(s)$ 表示在状态 $s$ 下按照策略 $"pi"$ 行动所能获得的期望回报：

$ V^"pi"(s) = E_"pi" [sum_(k=0)^infinity "gamma"^k r_(t+k) | s_t = s] $

动作价值函数 $Q^"pi"(s, a)$ 表示在状态 $s$ 下先采取动作 $a$，随后按照策略 $"pi"$ 行动所能获得的期望回报：

$ Q^"pi"(s, a) = E_"pi" [sum_(k=0)^infinity "gamma"^k r_(t+k) | s_t = s, a_t = a] $

在 Actor-Critic 类方法中，Actor 负责学习策略，Critic 负责估计价值函数或动作价值函数。本文涉及的 DDPG 和 PPO 都可以从 Actor-Critic 思路理解，只是它们在策略更新方式、价值估计方式和样本使用方式上存在差异。

从推荐系统角度看，策略决定“当前状态下推荐什么”，价值函数则评估“当前状态或动作对长期收益是否有利”。例如，一个推荐动作可能带来较高即时点击，但如果它使用户很快离开，则其长期价值未必高。价值函数的作用就是在训练过程中为策略提供更长远的反馈信号。

=== 探索与利用

探索与利用是强化学习中的基本矛盾。利用是指根据当前已学到的知识选择预期收益最高的动作；探索则是指尝试新的动作，以发现潜在更优的策略。如果智能体过度利用，可能陷入局部最优；如果过度探索，又可能长期选择低质量动作，影响学习效率。

在推荐系统中，探索与利用问题尤为重要。系统既需要向用户推荐当前模型认为最可能产生点击的内容，也需要适度探索用户潜在兴趣，以避免推荐结果过度单一。然而，不合理的探索可能降低用户体验，因此推荐场景中的探索通常需要更加谨慎。Virtual Taobao 这样的虚拟环境能够在不影响真实用户的情况下进行策略训练，为强化学习推荐算法研究提供了较为安全的实验条件。

不同算法处理探索的方式并不相同。DDPG 通常通过在动作上加入噪声进行探索，PPO 则通过随机策略分布采样动作。LinUCB 不是完整强化学习算法，但它通过置信上界在“已知高收益动作”和“不确定动作”之间进行权衡，也体现了探索与利用思想。本文将这些方法放在同一环境中比较，可以观察不同探索机制在连续动作推荐环境中的表现差异。

== 深度强化学习基础

=== 深度学习与神经网络基础

深度学习通过多层神经网络对复杂函数进行近似。一个典型前馈神经网络由输入层、若干隐藏层和输出层组成。每一层通常包含线性变换和非线性激活函数，其基本形式可以表示为：

$ h_l = "sigma"(W_l h_(l-1) + b_l) $

其中，$h_l$ 表示第 $l$ 层的隐藏表示，$W_l$ 和 $b_l$ 分别表示权重矩阵和偏置项，$"sigma"(x)$ 表示非线性激活函数。通过多层非线性变换，神经网络能够拟合复杂的状态—动作映射或价值函数。

在深度强化学习中，神经网络通常用于近似策略函数、价值函数或动作价值函数。例如，策略网络可以根据当前状态输出动作，价值网络可以估计当前状态的长期收益。与表格型强化学习方法相比，深度神经网络能够处理高维连续状态空间，因此更适合用于复杂推荐环境。

=== 多层感知机模型

多层感知机（Multi-Layer Perceptron，MLP）是一类基础的前馈神经网络模型。MLP 通常由若干全连接层和激活函数组成，能够将输入向量映射为输出向量。在本文研究中，MLP 被用于构造监督学习行为克隆策略网络，也可以作为强化学习算法中的策略网络或价值网络结构。

对于 SL-BC 方法，MLPPolicy 的输入是 91 维状态向量，输出是 27 维连续动作向量。为了匹配 VirtualTB-v0 的动作空间，网络最后一层使用 tanh 激活函数，将动作限制在 $[-1, 1]$ 范围内。该设计使监督学习得到的策略能够直接放入 VirtualTB-v0 环境中进行完整 episode 评估。

对于本文的状态输入和连续动作输出而言，MLP 可以作为状态到动作的函数近似器，也可以作为 Actor-Critic 方法中的策略网络或价值网络。其结构相对清晰，便于与状态归一化、动作范围约束和 LayerNorm 等训练设置结合使用。因此，本文在监督学习基线、PPO-Rec 和 DDPG-Rec 中均采用以 MLP 为基础的网络结构，以保证不同方法在表示能力上具有相对一致的比较基础。

=== 深度强化学习基本思想

深度强化学习将深度学习的函数近似能力与强化学习的序列决策能力结合起来。在状态空间或动作空间较大时，传统表格方法难以直接存储每个状态或动作的价值，而深度神经网络可以通过参数共享学习更一般的表示。DQN 使用深度神经网络近似动作价值函数，在高维输入任务中取得了重要进展@Mnih2015DQN。此后，策略梯度、Actor-Critic 和连续动作控制方法不断发展，为复杂环境中的策略学习提供了基础。

推荐系统中的用户状态通常包含用户画像、历史行为、上下文信息等多维特征，动作也可能不是离散物品编号，而是连续推荐向量或排序策略参数。因此，深度强化学习为连续动作推荐场景提供了一种可行建模方式。本文选择 DDPG 和 PPO 作为主实验中的深度强化学习算法，原因在于二者分别代表 off-policy 确定性策略梯度和 on-policy 策略优化两类不同思路，适合用于分析连续动作推荐环境中的样本效率与训练稳定性差异。

深度强化学习在推荐系统中的优势主要体现在两个方面。第一，它可以通过环境交互直接优化策略，而不只是在固定数据集上拟合历史行为；第二，它能够把多步交互中的累计 reward 纳入训练目标，从而为长期收益建模提供可能。但深度强化学习也存在训练样本需求大、训练过程不稳定、对超参数敏感等问题。因此，本文不会单纯假设深度强化学习一定优于传统方法，而是通过统一实验对比其实际表现。

=== 策略梯度与 Actor-Critic 方法

策略梯度方法直接对策略参数进行优化。设策略参数为 $"theta"$，策略目标为 $J("theta")$，则策略梯度方法通过估计 $"grad"_"theta" J("theta")$ 来更新策略。与基于价值函数选择动作的方法相比，策略梯度方法能够直接处理连续动作空间和随机策略，因此在连续控制问题中具有重要作用。

Actor-Critic 方法结合了策略梯度和价值估计。Actor 表示策略网络，负责根据状态选择动作；Critic 表示价值网络，负责评估当前策略或动作的好坏。Critic 的估计结果可以降低策略梯度的方差，提高训练效率。本文使用的 DDPG 和 PPO 都可以归入 Actor-Critic 框架，但它们的样本利用方式和策略更新机制有所不同。

在连续动作推荐任务中，Actor-Critic 框架具有较好的适用性。一方面，Actor 可以直接输出连续推荐动作或连续动作分布参数，避免枚举全部候选动作；另一方面，Critic 可以根据当前状态和反馈估计策略长期收益，为策略更新提供比单步点击更稳定的训练信号。因此，Actor-Critic 是本文理解 PPO、DDPG 以及 PPO-Rec、DDPG-Rec 的基础框架。

=== 连续动作强化学习算法

==== (1) PPO 算法

PPO（Proximal Policy Optimization）是一种常用的 on-policy 策略优化算法@Schulman2017PPO。PPO 的核心思想是在更新策略时限制新旧策略之间的变化幅度，以避免策略更新过大导致训练不稳定。相比 TRPO 使用复杂的约束优化形式@Schulman2015TRPO，PPO 采用裁剪目标函数，使算法实现更加简单，也更适合在工程实验中作为稳定的策略优化基线。

PPO 使用当前策略采集一批轨迹数据，并根据这些数据估计优势函数。优势函数反映某一动作相对于当前状态平均水平的好坏，如果优势为正，说明该动作相对有利；如果优势为负，说明该动作不应被进一步强化。设新旧策略概率比为：

$ r_t("theta") = frac(pi_theta(a_t | s_t), pi_"old"(a_t | s_t)) $

裁剪后的概率比定义为：

$ r_t^"clip"("theta") = "clip"(r_t("theta"), 1 - "epsilon", 1 + "epsilon") $

则 PPO 的裁剪目标可以写为：

$ L^"CLIP"("theta") = E_t [min(r_t("theta") A_t, r_t^"clip"("theta") A_t)] $

其中，$A_t$ 表示优势函数估计，$"epsilon"$ 表示裁剪范围。当新策略与旧策略差异过大时，裁剪函数会限制目标函数的变化，从而降低策略突然崩溃的风险。实际训练中，PPO 通常同时优化策略损失、价值函数损失和熵正则项：策略损失用于提升高优势动作概率，价值函数损失用于改进 Critic 估计，熵正则项用于保持一定探索能力。

对于本文的连续动作推荐任务，PPO 的优点是训练过程相对稳定，适合作为标准深度强化学习对照和 PPO-Rec 的基础算法；其不足是 on-policy 机制对样本利用率要求较高，每一批数据通常只能用于有限次数的策略更新。因此，在 VirtualTB-v0 中，PPO 的表现既受策略更新稳定性影响，也受采样效率和优势估计质量影响。

==== (2) DDPG 算法

DDPG（Deep Deterministic Policy Gradient）是一种面向连续动作空间的 off-policy Actor-Critic 算法@Lillicrap2015DDPG。DDPG 使用确定性策略网络直接输出连续动作，并使用 Critic 网络估计动作价值函数。与随机策略不同，DDPG 的 Actor 可以表示为 $a = mu_theta(s)$，即在给定状态下直接生成一个确定性动作向量。

DDPG 的 Critic 通过 Bellman 目标学习动作价值函数。对经验回放中的样本 $(s_i, a_i, r_i, s_(i+1))$，目标值可以写为：

$ y_i = r_i + gamma Q_(phi^-)(s_(i+1), mu_(theta^-)(s_(i+1))) $

其中，$Q_(phi^-)$ 和 $mu_(theta^-)$ 分别表示目标 Critic 和目标 Actor，$gamma$ 为折扣因子。Critic 通过最小化如下损失进行更新：

$ L(phi) = frac(1, N) sum_i (Q_phi(s_i, a_i) - y_i)^2 $

Actor 则通过最大化 Critic 对当前策略动作的价值估计进行更新：

$ J(theta) = frac(1, N) sum_i Q_phi(s_i, mu_theta(s_i)) $

为了提高训练稳定性，DDPG 通常引入经验回放和目标网络机制。经验回放可以打破连续样本之间的强相关性，并重复利用历史交互样本；目标网络则通过缓慢更新参数减少目标值剧烈变化。目标网络的软更新形式通常为：

$ theta^- <- tau theta + (1 - tau) theta^- $

其中，$tau$ 为较小的更新系数。训练时，DDPG 通常还会在 Actor 输出动作上加入噪声，以弥补确定性策略本身探索不足的问题。

在连续动作推荐环境中，动作是 27 维连续向量，DDPG 可以直接学习从状态到动作向量的映射。与 PPO 相比，DDPG 使用 off-policy 方式更新模型，能够重复利用历史交互样本，因此在样本效率方面具有一定优势。但 DDPG 对超参数、探索噪声和价值估计误差较为敏感，训练结果可能存在较大方差。

从算法特点看，PPO 和 DDPG 分别代表了不同的连续动作强化学习思路。PPO 强调策略更新的稳定性，但每次更新主要使用当前策略采集的数据；DDPG 可以通过经验回放重复利用历史样本，样本效率通常较高，但也可能更依赖经验回放、目标网络和探索噪声等训练设置。本文将二者同时纳入主实验，有助于观察 on-policy 与 off-policy 方法在 VirtualTB-v0 中的差异。

#captab(
  caption: [PPO 与 DDPG 算法机制对比],
  label: <tab-ppo-ddpg-compare>,
)[
  | 对比维度 | PPO | DDPG |
  | --- | --- | --- |
  | 策略类型 | 随机策略，输出动作分布 | 确定性策略，直接输出连续动作 |
  | 样本使用方式 | on-policy，主要使用当前策略采样数据 | off-policy，可使用经验回放重复利用历史数据 |
  | 价值估计 | 通常估计状态价值 $V(s)$ | 估计动作价值 $Q(s,a)$ |
  | 稳定机制 | 裁剪目标限制策略更新幅度 | 目标网络、经验回放和软更新 |
  | 探索方式 | 从策略分布中采样动作 | 在确定性动作上加入噪声 |
  | 本文作用 | 标准对照和 PPO-Rec 基础 | 标准对照和 DDPG-Rec 基础 |
]

@tab-ppo-ddpg-compare 概括了 PPO 与 DDPG 在策略形式、样本使用方式、价值估计和稳定机制等方面的差异。该对比也解释了后续实验中两类算法表现差异的来源：PPO 更强调单次策略更新的稳定性，DDPG 更强调连续动作输出和历史样本复用。

== 强化学习推荐系统建模

=== 推荐过程的强化学习建模

将推荐过程建模为强化学习问题时，需要明确状态、动作、奖励和终止条件。在本文中，状态表示当前用户与会话信息，动作表示推荐策略输出的连续推荐向量，奖励表示环境根据用户反馈返回的点击数，episode 表示一次用户会话。当用户离开或达到最大页面数时，当前 episode 结束。

在这一建模方式下，推荐系统不再只是预测某个候选物品的点击概率，而是需要在连续交互中学习策略。每一步推荐动作都可能影响用户下一步反馈和会话是否继续，因此策略优化需要考虑多步累计收益。该特点使强化学习推荐与传统单步点击率预测存在本质区别。

推荐任务与强化学习要素之间的对应关系如@fig-rl-rec-mapping 所示。该图从推荐场景中的用户状态、推荐动作、用户反馈和会话过程出发，对应到强化学习中的状态、动作、奖励、状态转移和 episode，有助于说明本文将虚拟电商推荐过程建模为马尔可夫决策过程的依据。

#capfig(
  image("figures/rl_rec_mapping.png", width: 70%),
  caption: [推荐任务与强化学习要素对应关系],
  label: <fig-rl-rec-mapping>,
)

#captab(
  caption: [本文推荐任务与强化学习要素的对应关系],
  label: <tab-rl-rec-mapping>,
)[
  | 强化学习要素 | 推荐场景含义 | 本文环境中的形式 |
  | --- | --- | --- |
  | 状态 | 当前用户和会话信息 | 91 维 observation |
  | 动作 | 推荐策略输出 | 27 维连续 action |
  | 奖励 | 用户即时反馈 | 点击数 reward |
  | 策略 | 推荐决策规则 | Random、SL-BC、LinUCB、DRL 策略 |
  | episode | 一次用户会话 | 用户离开或达到最大页数结束 |
]

=== 强化学习推荐与传统推荐的区别

传统推荐方法通常依赖离线历史数据，通过监督学习或相似性计算预测用户对物品的偏好。这类方法在点击率预测、评分预测和排序任务中效果较好，但通常将每条样本视为相对独立的数据点，难以显式建模动作对后续状态的影响。

强化学习推荐方法则将推荐系统视为智能体，将用户反馈视为环境响应，通过策略与环境的多轮交互优化长期累计回报。它能够自然处理延迟反馈、探索与利用以及长期收益优化问题。不过，强化学习方法也存在训练样本需求大、训练不稳定、在线试错风险高等问题。因此，本文采用 Virtual Taobao 虚拟环境进行离线训练和评估，以降低真实平台实验风险。

本文涉及的几类方法在学习目标上存在明显差异。SL-BC 主要拟合离线日志中的行为动作，LinUCB 根据即时 reward 更新线性模型，深度强化学习方法则通过环境交互优化累计 reward。三类方法的目标并不完全相同，因此在实验分析中不能只看单一指标，而需要结合 CTR、reward、动作范数和训练稳定性进行综合判断。

=== 推荐系统中的奖励设计

奖励函数是强化学习推荐系统中的关键设计。不同奖励定义会引导策略学习不同的行为。例如，如果只使用点击作为奖励，算法可能更关注提高点击率；如果同时考虑购买、停留时长或用户满意度，策略目标会更加复杂。在 VirtualTB-v0 环境中，本文使用环境原始返回的点击数作为 reward，并基于该 reward 统计累计奖励。

== 传统推荐基线方法

=== 随机策略基线

随机策略是不进行学习的基础 baseline。该方法在每一步从动作空间中随机采样连续动作，并将其输入 VirtualTB-v0 环境。随机策略通常不会取得较好推荐效果，但它能够提供一个下界参照，用于判断其他算法是否真正从环境反馈或离线数据中学习到了有效策略。

在本文主实验中，Random 作为 baseline 方法保留。Zero、Mean Action 等固定动作策略也可以作为补充参照，但由于其与本文主要研究问题关联较弱，因此不进入主实验表格。

=== 监督学习行为克隆方法

监督学习行为克隆（SL-BC）将推荐策略学习转化为监督学习问题。其基本思想是从离线日志中收集状态、动作和反馈数据，然后训练一个策略网络，使其输出动作尽可能接近日志中的行为动作。行为克隆早期已被用于自动驾驶等连续决策任务，用监督学习方式从示范行为中学习控制策略@Pomerleau1988ALVINN。本文使用 MLPPolicy 作为 SL-BC 策略网络，输入为 91 维状态，输出为 27 维动作，并通过 tanh 将动作限制在 $[-1, 1]$。

与强化学习不同，SL-BC 不通过与环境交互直接优化长期累计回报，而是学习离线行为数据中的动作模式。本文的 SL 数据由 VirtualTB-v0 中的行为策略采集，用于构造传统监督学习 baseline。训练时使用点击加权均方误差，使点击反馈较高的样本在损失函数中具有更大权重。评估时，SL-BC 策略必须放回 VirtualTB-v0 环境中运行完整 episode，而不能仅根据训练集 MSE 判断推荐效果。

=== 上下文 Bandit 方法

上下文 Bandit 是介于监督学习和强化学习之间的一类方法。它在每一步根据当前上下文选择动作，并根据即时奖励更新模型。与完整强化学习不同，Bandit 方法通常不建模动作对未来状态的长期影响，因此更适合用作只优化即时反馈的在线推荐 baseline。

本文使用 LinUCB 作为上下文 Bandit 方法。LinUCB 已被用于个性化新闻推荐等场景，其核心是根据上下文特征估计动作收益并加入置信上界以平衡探索与利用@Li2010LinUCB。本文的 LinUCB 在固定候选动作集合中选择具有较高置信上界的动作，并根据即时 reward 更新参数。候选动作集合由随机动作原型和来自 SL 数据集的高点击动作原型构成，动作会被裁剪到 $[-1, 1]$ 范围内。由于 LinUCB 只优化即时 reward，不考虑长期累计回报，本文将其作为与深度强化学习方法对照的传统在线学习基线。

由此可见，Random、SL-BC 和 LinUCB 分别提供了三种不同层次的传统参照：Random 表示无学习策略，SL-BC 表示从离线行为数据中学习，LinUCB 表示根据即时反馈在线更新。这三类方法不能完全覆盖所有传统推荐算法，但足以为本文分析深度强化学习方法提供基础对比。

== Virtual Taobao 实验平台

=== Virtual Taobao 平台概述

在真实推荐系统中，用户行为具有高度复杂性和不确定性。推荐系统每一次给出的推荐结果都会影响用户后续的点击、浏览、购买或离开等行为，因此推荐过程天然具有序列决策特征。深度强化学习虽然适合处理这类连续决策问题，但在真实电商平台中直接训练强化学习策略存在较大困难。一方面，强化学习算法通常需要大量环境交互样本；另一方面，不成熟的推荐策略如果直接部署到真实平台，可能会降低用户体验，并带来商业风险。因此，构建能够模拟真实用户行为的虚拟环境，是强化学习推荐算法研究中的重要问题。

Virtual Taobao 是 Shi 等人提出的面向真实电商搜索场景的虚拟实验平台，其目标是在不直接影响真实用户的情况下，为强化学习推荐策略提供离线训练和评估环境。该方法首先利用历史用户行为数据构建虚拟用户环境，然后在该虚拟环境中训练推荐策略，使智能体能够通过与虚拟用户交互学习推荐行为。原始 Virtual Taobao 论文指出，直接在真实淘宝平台中进行强化学习训练具有较高的物理成本，因此其基本思路是先构建一个能够模拟真实电商交互过程的虚拟淘宝环境，再在该环境中训练强化学习策略。

从强化学习建模角度看，Virtual Taobao 将电商推荐或商品搜索过程抽象为智能体与环境之间的交互过程。其中，推荐系统或搜索引擎可以看作智能体，用户及其反馈行为可以看作环境。智能体根据当前用户状态选择推荐动作，用户根据推荐结果产生点击、翻页、购买或离开等反馈，环境再将反馈结果返回给智能体。通过这种方式，推荐任务可以从传统的单步预测问题转化为连续多步的策略优化问题。

=== Virtual Taobao 的基本组成

原始 Virtual Taobao 平台主要包括用户生成、交互生成和强化学习策略训练三个部分。首先，平台需要模拟真实电商系统中的用户分布，即生成具有不同兴趣偏好、购买能力和搜索请求的虚拟用户。为此，原论文提出 GAN-SD 方法，用于从历史数据中学习用户分布，使生成的虚拟用户尽可能接近真实用户群体。其次，平台需要模拟用户与推荐系统之间的交互过程，即在给定推荐结果后，虚拟用户能够产生相应反馈。原论文提出 MAIL 方法，通过多智能体对抗模仿学习建模用户与平台之间的交互，使虚拟环境能够更好地反映真实平台中的用户行为。最后，在构建好的虚拟环境中，可以使用强化学习算法训练推荐策略，并通过虚拟环境反馈的奖励信号不断优化策略。

Virtual Taobao 的整体结构可以理解为：用户生成模型负责生成虚拟用户，交互模型负责模拟用户对推荐动作的反馈，强化学习算法则负责根据环境反馈优化推荐策略。原论文还指出，当强化学习算法在虚拟环境中训练时，策略可能会过度适应模拟器，从而导致在真实环境中泛化能力下降。为缓解这一问题，原论文提出了 Action Norm Constraint，即动作范数约束方法，对过大的动作施加惩罚，以减少策略对虚拟环境的过拟合。

需要说明的是，Virtual Taobao 并不是一个普通的静态推荐数据集，而是一个可交互的虚拟推荐环境。传统推荐数据集通常提供用户、物品和历史交互记录，算法主要在离线数据上进行评分预测或点击率预测；而 Virtual Taobao 则允许推荐策略在环境中连续做出动作，并根据用户反馈获得奖励。这一特性使其更加适合用于研究强化学习推荐算法。

=== 本文的环境形式

本文以 Virtual Taobao 作为实验平台，重点研究不同推荐策略算法在虚拟电商环境中的表现。与原始论文中完整的 Virtual Taobao 训练流程不同，本文不重新训练用户生成模型和用户交互模型，而是在已有虚拟环境基础上开展推荐策略算法研究。也就是说，本文将 Virtual Taobao 作为一个固定的离线模拟环境，主要关注不同算法在相同环境中的训练效果和评估结果。

在本文实验中，推荐过程被建模为一个马尔可夫决策过程。智能体在每一步接收当前用户状态，并输出一个连续动作向量作为推荐策略的决策结果。环境根据该动作模拟用户反馈，并返回相应奖励。本文所使用的环境状态由用户特征、上一轮反馈信息以及当前页面位置等组成；动作空间为连续向量空间，用于表示推荐策略输出的推荐动作；奖励信号主要来源于用户反馈中的点击信息。

#capfig(
  image("figures/virtualtb_env_design.png", width: 70%),
  caption: [VirtualTB-v0 虚拟电商环境交互流程图],
  label: <virtual-taobao-process>,
)

基于该环境，本文可以比较多类推荐方法的效果。一方面，可以使用随机策略、监督学习行为克隆方法以及上下文 Bandit 方法作为传统基线；另一方面，可以使用 DDPG 和 PPO 等深度强化学习方法进行策略训练。如@virtual-taobao-process 所示，VirtualTB-v0 由推荐智能体和虚拟用户环境闭环交互构成，环境内部复用原始 Virtual-Taobao 工作发布的预训练用户生成、反馈和离开模型。通过统一实验平台，不同算法能够在相同状态空间、动作空间和奖励定义下进行比较，从而提高实验结果的可比性。

== 实验评价标准

在推荐算法研究中，合理的评价指标对于分析算法性能具有重要作用。由于本文研究对象是虚拟电商环境下的推荐策略，算法输出的不只是单次点击预测结果，而是在一个完整用户会话中连续做出的推荐动作。因此，本文不仅关注单步反馈效果，也关注整个交互过程中的累计收益和策略稳定性，主要采用点击率、累计奖励、会话长度和动作范数等指标对不同算法进行评估。

=== 点击率

点击率是推荐系统中最常用的评价指标之一，通常用于衡量用户对推荐结果的兴趣程度。点击率越高，说明用户对推荐内容的响应越积极。在一般推荐系统中，点击率可以表示为：

$ C = frac(N_c, N_i) $ <eqt:ctr-basic>

在 Virtual Taobao 环境中，智能体每一步输出推荐动作后，环境会返回用户反馈。本文将一次页面交互视为一次展示，将环境返回的点击反馈作为点击次数的度量。因此，在一个完整用户会话中，可以根据总点击数和总交互页数计算平均点击率：

$ C = frac(1, T) sum_(t=1)^T c_t $ <eqt:ctr-episode>

其中，$C$ 表示点击率，$N_c$ 表示点击总次数，$N_i$ 表示展示总次数，$T$ 表示一个 episode 中的交互步数，$c_t$ 表示第 $t$ 步获得的点击反馈。当环境中的点击反馈为二值变量时，上式等价于“总点击数除以总展示数”；若环境中的点击反馈存在固定归一化方式，则在实验统计时按照环境定义进行统一处理。

点击率能够直观反映推荐策略的即时推荐效果。如果某一算法在相同实验条件下获得更高的点击率，说明该算法生成的推荐动作更容易引起用户点击。但点击率主要反映即时反馈，不能完全代表推荐策略的长期效果，因此还需要结合累计奖励等指标进行综合分析。

=== 累计奖励

强化学习的目标是最大化长期累计回报，因此累计奖励是评价强化学习推荐策略的重要指标。在 Virtual Taobao 环境中，智能体与虚拟用户进行多步交互，每一步都会获得环境返回的奖励。一个 episode 内所有奖励之和可以表示为：

$ R = sum_(t=1)^T r_t $ <eqt:cumulative-reward>

其中，$r_t$ 表示第 $t$ 步环境返回的奖励，$T$ 表示当前 episode 的长度。

累计奖励反映了推荐策略在一次完整用户会话中的总体收益。与单步点击率相比，累计奖励更强调策略在整个交互过程中的表现。如果某一算法能够在多个交互步骤中持续获得较高奖励，则其累计奖励也会更高。对于深度强化学习算法而言，累计奖励是衡量策略优化效果的核心指标之一。

在本文实验中，将统计不同算法在多个 episode 上的平均累计奖励和标准差。平均累计奖励用于衡量算法整体性能，标准差用于反映算法在不同用户会话或不同随机种子下的稳定性。

=== 会话长度

会话长度表示用户在一次 episode 中与推荐系统持续交互的步数。在电商推荐场景中，用户如果持续浏览更多页面，通常说明推荐结果在一定程度上维持了用户兴趣；如果用户很快离开，则可能说明推荐策略未能满足用户需求。因此，会话长度可以作为分析用户留存和交互持续性的辅助指标。

设一个 episode 的结束时间步为 T，则该 episode 的会话长度可以表示为：

$ L = T $ <eqt:session-length>

在 Virtual Taobao 环境中，episode 通常会在用户离开或达到最大页面数时结束。因此，会话长度能够反映推荐策略对用户继续浏览行为的影响。需要注意的是，会话长度并不能单独代表推荐效果。例如，用户浏览时间较长但点击较少，并不一定说明推荐策略更优。因此，本文将会话长度与点击率、累计奖励等指标结合分析。

=== 动作范数

Virtual Taobao 的动作空间是连续向量空间，推荐策略输出的是一个多维连续动作。对于连续动作强化学习算法而言，不同算法可能会学习到不同幅度和分布的动作。如果策略输出的动作过大，可能说明算法在虚拟环境中利用了某些模拟器偏差，从而产生过度激进的推荐行为。

为分析策略输出动作的稳定性，本文引入动作范数作为辅助评价指标。设第 $t$ 步策略输出的动作向量为 $a_t$，则其二范数可以表示为：

$ ||a_t||_2 = sqrt(sum_(i=1)^d a_(t, i)^2) $ <eqt:action-norm>

其中，$d$ 表示动作维度，$a_(t, i)$ 表示动作向量 $a_t$ 的第 $i$ 个分量。

在一个 episode 中，可以计算平均动作范数：

$ A = frac(1, T) sum_(t=1)^T ||a_t||_2 $ <eqt:avg-action-norm>


动作范数并不是直接的推荐效果指标，但可以用于观察策略行为是否稳定。连续动作策略如果长期输出幅度过大的动作，可能说明其对固定模拟器中的特定响应区域形成了较强依赖。因此，在本文实验中统计动作范数，有助于分析不同算法是否倾向于输出过大的连续动作，并为后续可能的动作约束改进提供依据。

== 本章小结

本章围绕推荐系统、强化学习、深度强化学习、强化学习推荐建模、传统基线方法和 Virtual Taobao 实验平台进行了介绍。推荐系统从静态偏好预测发展到多轮交互建模后，强化学习提供了一种处理长期收益优化问题的自然框架。Virtual Taobao 作为可交互的虚拟电商环境，使本文能够在固定模拟环境中比较多种推荐策略。后续章节将在本章理论基础上，进一步介绍本文的算法设计、实验框架和实验结果。

= 基于 VirtualTB-v0 的推荐算法设计与实现

本章在前文理论基础上，介绍本文如何将 Virtual Taobao 的预训练虚拟用户模型组织为可运行的强化学习推荐环境，并在该环境上设计传统基线、标准深度强化学习对照方法和面向连续动作推荐任务的改进策略。需要说明的是，本文没有重新训练 Virtual Taobao 的用户生成模型、用户反馈模型和离开模型，而是在原始 Virtual-Taobao 工作发布的预训练模型基础上，完成环境接口封装、状态—动作—奖励定义、策略训练流程和统一评估方案。

== 系统总体设计

本文的实验系统围绕 VirtualTB-v0 虚拟电商推荐环境展开。整体流程可以概括为：首先，环境生成一个虚拟用户并给出初始状态；其次，推荐策略根据当前状态输出连续动作；然后，环境根据用户特征、当前页码和推荐动作生成点击反馈，并判断当前用户是否离开；最后，算法根据交互反馈更新策略，评估阶段则在相同环境中统计点击率、累计奖励、会话长度和动作范数等指标。

从功能划分看，本文系统由环境建模、数据构造、策略学习和结果评估四个部分组成。环境建模部分负责将已有虚拟用户模型封装为符合 Gymnasium 规范的强化学习环境@Towers2024Gymnasium；数据构造部分用于形成监督学习和上下文 Bandit 所需的离线交互样本；策略学习部分包括传统基线、标准深度强化学习实现以及本文面向连续动作推荐任务设计的 PPO-Rec 和 DDPG-Rec；结果评估部分在统一随机种子和统一指标下比较不同方法的表现。

本文主实验方法如@tab-main-algorithms 所示。Random、MLP-SL 和 LinUCB 分别对应无学习策略、离线监督学习策略和只优化即时反馈的上下文 Bandit 方法；PPO-SB3 与 DDPG-SB3 用作通用深度强化学习实现的对照；PPO-Rec 与 DDPG-Rec 则是在相同推荐任务设定下，结合状态尺度、动作范围和短会话反馈特点设计的强化学习推荐策略。

#captab(
  caption: [本文主实验算法清单],
  label: <tab-main-algorithms>,
)[
  | 方法 | 类型 | 作用 |
  | --- | --- | --- |
  | Random | 无学习基线 | 随机采样连续动作，提供最基础的下界参照 |
  | MLP-SL | 监督学习基线 | 使用离线动作数据训练多层感知机策略 |
  | LinUCB | 上下文 Bandit | 基于固定动作原型和即时反馈进行在线选择 |
  | PPO-SB3 | 标准深度强化学习对照 | 使用通用 PPO 实现训练连续动作策略 |
  | DDPG-SB3 | 标准深度强化学习对照 | 使用通用 DDPG 实现训练连续动作策略 |
  | PPO-Rec | 推荐强化学习策略 | 面向连续动作推荐任务改进的 PPO 策略 |
  | DDPG-Rec | 推荐强化学习策略 | 面向短会话反馈和连续动作空间改进的 DDPG 策略 |
]

== 预训练环境模型来源

Virtual Taobao 的核心思想是用历史用户行为数据学习一个可交互的虚拟电商环境，使强化学习算法能够在不直接影响真实用户的情况下进行训练与评估@Shi2019VirtualTaobao。原始 Virtual-Taobao 工作基于匿名淘宝交互数据训练虚拟用户生成、用户反馈和用户离开相关模型，并发布了可供研究使用的预训练模拟器。本文使用的 VirtualTB-v0 即建立在这些已有模型之上。

具体而言，VirtualTB-v0 内部包含三个与用户行为模拟相关的模型，如@tab-virtualtb-model-source 所示。用户生成模型用于在每个 episode 开始时生成虚拟用户画像；反馈模型用于根据用户画像、当前页码和推荐动作生成点击反馈；离开模型用于决定当前用户在第几页离开。本文没有重新训练这三个模型，也没有直接使用真实淘宝原始日志训练环境，而是复用原始 Virtual-Taobao 发布实现中的预训练模型权重，并将其组织为统一的强化学习环境接口。

#captab(
  caption: [VirtualTB-v0 内部预训练模型来源与作用],
  label: <tab-virtualtb-model-source>,
)[
  | 模型 | 输入 | 输出 | 在环境中的作用 | 来源 |
  | --- | --- | --- | --- | --- |
  | UserModel | 随机噪声向量 | 88 维虚拟用户画像 | 生成每个 episode 的用户状态基础特征 | 原始 Virtual-Taobao 预训练模型 |
  | ActionModel | 用户画像、当前页码、推荐动作 | 点击数和反馈索引 | 模拟用户对推荐动作的即时反馈 | 原始 Virtual-Taobao 预训练模型 |
  | LeaveModel | 用户画像 | 用户离开页数 | 决定 episode 的自然终止时刻 | 原始 Virtual-Taobao 预训练模型 |
]

这一来源说明对于本文实验解释十分重要。本文的实验对象不是一个人工虚构的点击函数，而是原始 Virtual-Taobao 工作根据真实匿名交互数据学习得到的虚拟电商模拟器；同时，本文的工作边界也必须保持清晰，即本文关注的是该模拟器上的强化学习任务定义、环境接口封装和推荐策略训练，而不是重新构建 Virtual Taobao 的用户行为模型。

== VirtualTB-v0 环境接口设计

为了使 Virtual Taobao 能够被不同强化学习算法直接调用，本文将其封装为符合 Gymnasium 规范的环境。环境在开始一次交互时生成一个虚拟用户，并根据用户画像预测其离开页数；在每一步交互中，智能体观察当前状态，输出一个 27 维连续推荐动作，环境再根据推荐动作返回点击奖励、下一状态和终止标志。

该接口设计使推荐任务可以被表示为标准的强化学习交互过程：

$ s_t -> a_t -> r_t, s_(t+1) $

其中，$s_t$ 为当前用户和会话状态，$a_t$ 为推荐策略输出的连续动作，$r_t$ 为环境返回的点击反馈。与静态推荐数据集不同，VirtualTB-v0 允许策略在一个用户会话中连续做出动作，并根据模拟用户反馈不断更新推荐策略。

#captab(
  caption: [VirtualTB-v0 环境接口说明],
  label: <tab-env-interface>,
)[
  | 项目 | 定义 |
  | --- | --- |
  | 环境类型 | 符合 Gymnasium 规范的连续动作强化学习环境 |
  | 状态空间 | 91 维连续状态向量 |
  | 动作空间 | 27 维连续动作向量 |
  | 动作范围 | 每个维度限制在 $[-1, 1]$ |
  | 即时奖励 | 用户反馈模型生成的点击数 |
  | 终止条件 | 达到用户预测离开页或最大页数限制 |
  | 评估信息 | 累计点击数、页数、CTR 和离开页 |
]

从环境交互逻辑看，一次 episode 对应一个虚拟用户会话。环境首先生成用户画像 $u$，并根据该用户画像预测离开页 $l$。在第 $t$ 页，策略根据状态 $s_t$ 输出动作 $a_t$，反馈模型根据 $(u, t, a_t)$ 生成点击数和反馈索引，点击数作为 reward 返回给智能体。当当前页数达到离开页 $l$ 时，episode 自然结束；若页数达到最大限制，则 episode 被截断结束。

== 状态、动作与奖励函数设计

强化学习推荐任务的关键在于合理定义状态、动作和奖励。本文的设计目标不是还原完整真实电商系统的所有细节，而是在 VirtualTB-v0 固定模拟器中构造一个可训练、可评估、可复现的连续动作推荐任务。

=== 状态设计

VirtualTB-v0 的状态向量由用户静态特征、上一轮用户反馈和当前会话进度组成：

$ s_t = [u, c_(t-1), b_(t-1), p_t] $

其中，$u in R^88$ 表示当前虚拟用户画像，$c_(t-1)$ 表示上一轮点击数，$b_(t-1)$ 表示上一轮反馈索引，$p_t$ 表示当前页码。因此，状态总维度为 $88 + 2 + 1 = 91$。前 88 维用户画像由用户生成模型产生，后 3 维动态特征用于向策略提供上一轮反馈和当前会话位置。

这种状态设计体现了推荐任务中的两个基本信息来源：一是用户自身属性，二是用户在当前会话中的近期反馈。由于 VirtualTB-v0 没有显式提供用户历史点击序列、候选商品集合或商品编号，本文没有引入 RNN 或 Transformer 建模长期兴趣序列，而是将环境可观测信息直接作为强化学习状态。

=== 动作设计

VirtualTB-v0 的动作是 27 维连续向量：

$ a_t in [-1, 1]^27 $

在原始 Virtual Taobao 设定中，推荐系统需要输出一个动作向量，用于影响虚拟用户反馈模型对当前推荐结果的响应。本文沿用这一连续动作形式，将推荐策略的输出直接视为推荐动作。所有策略输出的动作都会被限制在 $[-1,1]$ 范围内，以保证动作满足环境定义。

连续动作设计使得 DDPG、PPO 等连续控制算法能够自然应用于推荐任务。与传统推荐系统中直接选择商品编号不同，本文动作并不表示某个具体商品，而是模拟推荐系统在虚拟环境中提交给用户反馈模型的连续推荐表示。因此，本文更关注策略如何在连续动作空间中寻找能够带来较高点击反馈的动作区域。

=== 奖励函数设计

本文使用环境反馈模型返回的点击数作为即时奖励：

$ r_t = c_t $

其中，$c_t$ 表示第 $t$ 步推荐动作获得的点击反馈。一个 episode 的累计奖励为：

$ R = sum_(t=1)^T r_t $

点击奖励能够直接反映虚拟用户对当前推荐动作的即时响应，也是本文计算 CTR 的基础。需要注意的是，本文没有额外加入人工 reward shaping，也没有用动作范数惩罚替代原始环境奖励。这样做的目的是保证不同算法在评估阶段都基于相同的原始环境反馈进行比较。

在评估时，本文进一步统计平均 CTR：

$ "CTR" = frac(sum_(t=1)^T c_t, 10T) $

其中，分母中的 10 来自 VirtualTB-v0 中点击反馈的取值尺度。该 CTR 是固定模拟环境下的评价指标，不能直接等同于真实线上电商平台中的点击率。

== 离线日志与监督学习数据集

为了构造监督学习行为克隆 baseline，本文使用 VirtualTB-v0 中的离线交互样本训练 MLP-SL 策略。该数据用于构造传统监督学习对照方法，而不是作为专家策略或最优策略数据。离线样本共 100000 条，包含状态、动作和点击反馈，能够支持策略网络学习从状态到连续动作的映射。

离线数据的主要字段如@tab-sl-dataset-fields 所示。每条样本记录一个交互步中的状态、执行动作和用户反馈。由于这些数据来自 VirtualTB-v0 或原始示例中的监督学习数据，论文中将其称为“虚拟环境离线监督学习数据”，而不称为真实淘宝日志。

#captab(
  caption: [监督学习数据字段说明],
  label: <tab-sl-dataset-fields>,
)[
  | 字段 | 形状或含义 |
  | --- | --- |
  | states | $(N, 91)$，环境状态 |
  | actions | $(N, 27)$，行为策略产生的连续动作 |
  | clicks | $N$，点击反馈 |
  | rewards | $N$，环境即时奖励 |
  | episode_ids | $N$，样本所属 episode 编号 |
  | step_ids | $N$，样本在 episode 中的步编号 |
]

== 传统基线方法设计

由于 VirtualTB-v0 的动作不是离散商品编号，而是 27 维连续动作向量，本文无法直接构造标准用户--物品交互矩阵，因此 UserCF、ItemCF 和矩阵分解等方法难以自然作为主实验方法。基于这一任务特点，本文传统方法部分选择 Random、MLP-SL 和 LinUCB，分别代表无学习策略、离线监督学习策略和即时反馈在线学习策略。

=== Random

Random 策略在每个时间步从动作空间中随机采样动作，并直接输入环境。该方法不使用训练数据，也不根据环境反馈更新策略。Random 的作用是提供无学习情况下的基础参照，用于判断其他方法是否真正利用了状态信息、离线数据或环境反馈。

由于 Random 不依赖状态特征，其不同随机种子下的表现通常较为稳定，但指标水平较低。在本文实验中，Random 不作为可部署推荐策略，而是作为所有学习方法需要超过的下界。

=== MLP-SL

MLP-SL 将推荐策略学习转化为监督学习问题。其输入为 91 维状态向量，输出为 27 维连续动作向量，并通过 tanh 激活函数将输出限制在 $[-1,1]$ 范围内。训练目标是使模型输出动作尽可能接近离线样本中的行为动作。

设模型预测动作为 $hat(a)$，离线样本中的动作为 $a$，点击反馈为 $c$。为了使点击反馈较高的样本在训练中具有更大影响，本文使用点击加权均方误差，其样本权重定义为：

$ w = 1 + alpha frac(c, 10) $

其中，$alpha$ 为点击权重系数，本文实验中取 $alpha = 1.0$。需要指出的是，MLP-SL 的训练目标是拟合离线动作，而不是直接最大化环境中的累计奖励。因此，即使监督学习损失下降，也不一定意味着放回环境评估时 CTR 必然提升。本文将 MLP-SL 作为离线监督学习 baseline，以观察单纯行为拟合与环境交互优化之间的差异。

=== LinUCB

LinUCB 是本文采用的上下文 Bandit baseline。它根据当前状态估计不同候选动作的即时收益，并加入置信上界项平衡探索与利用。与完整强化学习不同，LinUCB 不显式建模动作对未来状态和累计回报的影响，因此适合作为“只优化即时反馈”的在线学习对照。

由于 VirtualTB-v0 的动作空间是连续空间，LinUCB 不能直接枚举所有可能动作。本文使用固定动作原型集合近似候选动作空间。候选动作由随机动作和离线样本中的高反馈动作构成，并统一限制在环境允许范围内。在每一步，LinUCB 根据当前状态和候选动作计算上置信界，选择估计收益较高的动作执行，并根据即时奖励更新参数。

== 标准深度强化学习对照方法

为了判断面向推荐任务的策略改进是否真正带来收益，本文引入 Stable-Baselines3 中的 PPO 和 DDPG 作为标准深度强化学习对照@Raffin2021SB3。二者都能够处理连续动作空间，但训练机制不同：PPO 属于 on-policy 策略优化方法，强调限制策略更新幅度；DDPG 属于 off-policy Actor-Critic 方法，使用经验回放和目标网络提高样本利用效率。

在本文中，PPO-SB3 和 DDPG-SB3 使用相同的 VirtualTB-v0 环境、相同的状态与动作定义以及相同的最终评估指标。它们的作用不是作为本文提出的方法，而是作为通用强化学习实现的参考对照。通过比较 SB3 对照与 PPO-Rec、DDPG-Rec，可以观察通用算法直接应用于连续动作推荐任务时的表现，以及推荐任务特性对训练效果的影响。

== PPO-Rec 推荐策略

PPO-Rec 在 PPO 基本思想基础上，面向连续动作推荐任务中的状态尺度差异和动作范围约束进行设计。策略网络采用 Actor-Critic 结构，输入为 91 维状态，输出为连续高斯策略的均值和状态价值估计。策略均值经过 tanh 限制在环境动作范围内，训练时从高斯分布中采样动作以保持探索，评估时使用确定性均值动作以减少随机性。

由于推荐状态中包含点击数、反馈索引和页码等不同尺度的动态特征，PPO-Rec 对状态中的动态维度进行归一化处理：上一轮点击反馈除以 10，反馈索引除以 9，页码除以 100，使其与用户静态特征处于更接近的数值范围。同时，网络中使用 LayerNorm 缓解特征尺度差异带来的训练不稳定问题。

PPO-Rec 的策略优化仍采用 PPO 裁剪目标函数。设新旧策略概率比为 $r_t(theta)$，优势估计为 $A_t$，裁剪范围为 $epsilon$，并定义：

$ r_t^"clip"(theta) = "clip"(r_t(theta), 1 - epsilon, 1 + epsilon) $

则策略目标为：

$ L^"CLIP"(theta) = E_t [min(r_t(theta) A_t, r_t^"clip"(theta) A_t)] $

在训练过程中，PPO-Rec 同时优化策略损失、价值函数损失和熵正则项。由于虚拟电商推荐中的用户会话长度相对较短，本文使用较小折扣因子，使策略更加关注近期点击反馈。训练期间定期将当前策略放回环境中进行完整 episode 评估，并以 CTR 作为保存最优策略的重要依据。

PPO-Rec 的整体训练结构如@fig-ppo-rec-structure 所示。该方法以归一化后的状态作为输入，通过 Actor-Critic 网络产生连续动作分布和价值估计，再将约束后的动作输入环境获得点击反馈，并利用 PPO 裁剪目标函数完成策略更新。

#capfig(
  image("figures/ppo_rec_structure.png", width: 70%),
  caption: [PPO-Rec 推荐策略结构图],
  label: <fig-ppo-rec-structure>,
)

== DDPG-Rec 推荐策略

DDPG-Rec 针对连续动作推荐任务中的短会话反馈和确定性动作输出特点进行设计。其 Actor 网络根据当前状态直接输出 27 维确定性动作，Critic 网络估计状态—动作对的价值。与 PPO 不同，DDPG 使用经验回放机制，可以重复利用历史交互样本，因此在样本效率方面具有潜在优势。

为了提高训练稳定性，DDPG-Rec 使用目标 Actor、目标 Critic 和软更新机制。设在线网络参数为 $theta$，目标网络参数为 $theta^-$，软更新可以表示为：

$ theta^- <- tau theta + (1 - tau) theta^- $

其中，$tau$ 为较小的更新系数。Critic 的目标值由即时奖励和目标网络估计的下一状态价值构成。考虑到 episode 终止状态不应继续累积下一状态价值，本文采用如下形式：

$ y_t = beta r_t + gamma (1 - d_t) Q^-(s_(t+1), mu^-(s_(t+1))) $

其中，$beta$ 为 reward scale 系数，$gamma$ 为折扣因子，$d_t$ 表示当前转移是否终止，$Q^-$ 和 $mu^-$ 分别表示目标 Critic 和目标 Actor。Actor 则通过最大化 Critic 对当前动作的价值估计进行更新：

$ J_mu = E_s [Q(s, mu(s))] $

在训练设置上，DDPG-Rec 使用状态归一化、LayerNorm、较小 Actor 学习率、梯度裁剪和延迟开始学习等设置，以降低训练初期不稳定性。训练时在动作上加入探索噪声，评估时使用确定性动作。与 PPO-Rec 类似，DDPG-Rec 在训练过程中定期进行环境评估，并根据 CTR 保存最优策略。

DDPG-Rec 的整体训练结构如@fig-ddpg-rec-structure 所示。该方法通过 Actor 输出确定性连续动作，并将交互样本存入经验回放池；Critic 根据回放样本估计状态--动作价值，目标网络和软更新机制用于缓解价值估计震荡。

#capfig(
  image("figures/ddpg_rec_structure.png", width: 70%),
  caption: [DDPG-Rec 推荐策略结构图],
  label: <fig-ddpg-rec-structure>,
)

@tab-rec-method-diff 从“对应问题--方法体现--预期作用”的角度总结了 PPO-Rec 和 DDPG-Rec 的主要改进点。需要说明的是，表中列出的内容属于面向连续动作推荐任务的训练流程与策略实现改进，而不是对 PPO 或 DDPG 基础理论的重新提出。

#captab(
  caption: [PPO-Rec 与 DDPG-Rec 的主要改进点],
  label: <tab-rec-method-diff>,
)[
  | 改进点 | 对应问题 | 在 PPO-Rec / DDPG-Rec 中的体现 | 预期作用 |
  | --- | --- | --- | --- |
  | 状态归一化 | 91 维状态中动态特征尺度不一致 | 对上一轮点击、反馈索引和页码进行归一化处理 | 缓解尺度差异，提升训练稳定性 |
  | 网络稳定化 | 高维状态输入可能造成策略和价值估计波动 | 使用 MLP 作为主体网络，并引入 LayerNorm | 降低训练震荡，提高参数更新稳定性 |
  | 动作范围约束 | 连续动作输出可能越界或幅度异常 | Actor 输出经 tanh 限制，环境侧保持动作裁剪 | 使推荐动作符合环境定义，避免异常动作影响反馈 |
  | 短会话参数设置 | 用户会话较短，远期回报不确定性较高 | PPO-Rec 使用较小 $gamma$ 和 GAE 参数；DDPG-Rec 使用较小 $gamma$ | 使策略更关注近期点击反馈和短期会话收益 |
  | 奖励缩放 | 点击反馈尺度可能影响 Critic 目标值 | DDPG-Rec 使用 reward scale 系数 $beta=0.5$ | 缓解价值估计波动，改善 Critic 学习 |
  | 基于 CTR 的策略选择 | 训练损失与最终推荐指标不完全一致 | 周期评估并按评估 CTR 保存最优策略 | 使模型选择更贴近最终评价指标 |
]

== 实验记录与统一评估

为了保证不同方法之间具有可比性，本文在评估阶段统一使用 VirtualTB-v0 的原始 reward 和 CTR 计算方式，不使用训练过程中可能出现的辅助损失或内部指标替代最终评估指标。每个算法均在 seed=1,2,3 下运行，并统计最终 CTR、平均累计奖励、会话长度、动作范数和 seed 间标准差。

对于深度强化学习方法，训练过程中还记录周期性评估结果，用于绘制 CTR 曲线、奖励曲线和动作范数曲线。对于 Random、MLP-SL 和 LinUCB 等非深度强化学习方法，本文只报告最终评估结果，不绘制随训练步数变化的曲线，以避免将不同类型方法的训练过程作不恰当比较。

== 本章小结

本章介绍了本文的推荐算法设计与实现方案。首先，本文说明了 VirtualTB-v0 环境的模型来源和工作边界，即复用原始 Virtual-Taobao 基于真实匿名交互数据训练得到的预训练模型，并将其封装为标准强化学习环境；其次，本文定义了状态、动作、奖励和终止条件，将虚拟电商推荐过程建模为连续动作强化学习任务；最后，本文设计了 Random、MLP-SL、LinUCB、PPO-SB3、DDPG-SB3、PPO-Rec 和 DDPG-Rec 等方法，为后续实验比较奠定基础。


= 实验结果与分析

== 实验设置

本文在 VirtualTB-v0 环境中对不同推荐策略进行统一评估。主实验包含三类方法：第一类为传统或非强化学习基线，包括 Random、MLP-SL 和 LinUCB；第二类为标准深度强化学习对照，包括 PPO-SB3 和 DDPG-SB3；第三类为本文面向连续动作推荐任务设计的 PPO-Rec 和 DDPG-Rec。所有方法均使用 seed=1、2、3 三个随机种子进行重复实验，并统计平均值和标准差。

实验评价指标包括平均 CTR、平均累计奖励、动作范数和不同随机种子下的稳定性。其中，CTR 用于衡量虚拟用户对推荐动作的点击反馈强度，平均累计奖励用于衡量完整 episode 中的总点击收益，动作范数用于观察连续动作策略的输出幅度。会话长度在实验中作为辅助记录指标保留，但由于各方法最终会话长度差异较小，正文不将其作为主要结论展开。需要说明的是，动作范数不是直接的推荐效果指标，而是辅助分析策略行为是否过于激进。

实验软件环境如@tab-software-env 所示。部分方法在不同批次中使用 CPU 或 CUDA 设备运行，因此本文不以真实训练耗时作为核心比较指标，而是以环境交互步数、最终评估 episode 数和统一评价指标作为主要比较依据。

#captab(
  caption: [实验软件环境],
  label: <tab-software-env>,
)[
  | 项目 | 版本或说明 |
  | --- | --- |
  | Python | 3.11 |
  | PyTorch | 2.x |
  | Gymnasium | 1.x |
  | Stable-Baselines3 | 2.x |
  | NumPy | 2.x |
  | 主要环境 | VirtualTB-v0 |
]

不同方法的训练预算如@tab-exp-setting 所示。PPO-SB3、DDPG-SB3 和 PPO-Rec 均训练 100 万步，DDPG-Rec 训练 30 万步。DDPG-Rec 的训练步数较少，是因为其在前期实验中已经表现出较高样本效率；在结果分析时，本文会同时说明这一训练预算差异，避免将不同训练设置下的结果作不加说明的直接比较。Random、MLP-SL 和 LinUCB 不具有与深度强化学习完全对应的环境交互训练曲线，因此主要报告最终评估结果。

#captab(
  caption: [主实验设置],
  label: <tab-exp-setting>,
)[
  | 方法 | 方法类别 | 训练步数 | 最终评估 episode 数 | 是否绘制训练曲线 |
  | --- | --- | --- | --- | --- |
  | Random | 无学习基线 | 无 | 300 | 否 |
  | MLP-SL | 监督学习基线 | 离线训练 | 300 | 否 |
  | LinUCB | 上下文 Bandit | 离线拟合与环境评估 | 300 | 否 |
  | PPO-SB3 | 标准 DRL 对照 | 1M | 300 | 是 |
  | DDPG-SB3 | 标准 DRL 对照 | 1M | 300 | 是 |
  | PPO-Rec | 推荐任务改进 DRL | 1M | 500 | 是 |
  | DDPG-Rec | 推荐任务改进 DRL | 300k | 500 | 是 |
]

进一步地，主要方法的关键参数如@tab-key-hparams 所示。该表只列出影响本文结果解释的核心参数，完整运行参数以实验记录文件为准。

#captab(
  caption: [主要方法关键参数],
  label: <tab-key-hparams>,
)[
  | 方法 | 关键参数 |
  | --- | --- |
  | MLP-SL | epoch=50，batch size=512，lr=0.001，reward weight=1.0，best model 按 eval CTR 选择 |
  | LinUCB | arms=32，alpha=0.5，ridge=1.0，候选动作来自随机原型与高反馈动作原型 |
  | PPO-SB3 | total timesteps=1M，eval freq=20000，final eval episodes=300 |
  | DDPG-SB3 | total timesteps=1M，eval freq=20000，final eval episodes=300 |
  | PPO-Rec | total timesteps=1M，eval freq=20000，gamma=0.5，gae lambda=0.9，clip range=0.2，final eval episodes=500 |
  | DDPG-Rec | total timesteps=300k，eval freq=5000，gamma=0.7，reward scale=0.5，actor lr=$3 times 10^(-5)$，critic lr=$3 times 10^(-4)$，final eval episodes=500 |
]

需要强调的是，@tab-exp-setting 和@tab-key-hparams 表明各方法训练预算和最终评估 episode 数并不完全相同。因此，本文对最终结果的解释是“在当前实验设置下的性能观察”，而不是严格意义上消除所有训练预算差异后的无偏公平比较。学习速度分析主要观察策略达到不同 CTR 阈值所需的交互步数；最终性能比较则主要依据训练结束后保存的最优策略在固定环境中的正式评估结果。因此，本文不将 DDPG-Rec 与 PPO-Rec 的最终 CTR 差异解释为算法本身的绝对优劣，而主要将其作为当前训练预算和参数配置下的实验观察。本文更关注面向推荐任务特点的策略实现相较通用 SB3 实现是否能够带来更稳定的性能提升。

== 主实验最终结果

@tab-main-results 给出了各方法在三个随机种子上的最终评估均值和标准差。可以看到，Random 的 CTR 仅为 0.0185，说明随机连续动作几乎不能获得有效点击反馈。MLP-SL 的 CTR 提升到 0.1699，表明离线动作拟合能够学习到一定行为模式，但其效果仍然有限。LinUCB 的 CTR 为 0.2222，高于 Random 和 MLP-SL，说明基于上下文和即时反馈的动作选择能够带来进一步提升，但由于其只在固定动作原型集合中选择动作，且不建模长期累计回报，整体性能仍低于深度强化学习方法。

标准深度强化学习对照中，PPO-SB3 的 CTR 为 0.4158，DDPG-SB3 的 CTR 为 0.3031。二者均高于传统基线，说明强化学习策略能够通过环境交互学习到更有效的连续动作。但与面向推荐任务改进后的策略相比，SB3 对照方法仍存在较大差距。PPO-Rec 的 CTR 达到 0.8670，DDPG-Rec 的 CTR 达到 0.8958，二者在平均累计奖励上也高于其他方法。

#captab(
  caption: [各算法最终评估结果],
  label: <tab-main-results>,
)[
  | 方法 | CTR | 平均奖励 | 动作范数 | 训练步数 |
  | --- | --- | --- | --- | --- |
  | Random | 0.0185 ± 0.0002 | 1.4567 ± 0.0033 | 2.9913 ± 0.0002 | 无 |
  | MLP-SL | 0.1699 ± 0.0676 | 21.1011 ± 4.4985 | 0.5634 ± 0.0956 | 离线训练 |
  | LinUCB | 0.2222 ± 0.0244 | 14.2411 ± 1.1145 | 3.7024 ± 0.0345 | 离线拟合 |
  | PPO-SB3 | 0.4158 ± 0.0413 | 28.6544 ± 4.7950 | 3.1917 ± 1.2221 | 1M |
  | DDPG-SB3 | 0.3031 ± 0.1101 | 20.4989 ± 7.1486 | 4.9396 ± 0.0403 | 1M |
  | PPO-Rec | 0.8670 ± 0.0529 | 65.4773 ± 3.0918 | 3.7742 ± 0.2963 | 1M |
  | DDPG-Rec | 0.8958 ± 0.0064 | 67.2553 ± 0.1504 | 4.7519 ± 0.0927 | 300k |
]

从最终 CTR 看，传统基线方法整体低于深度强化学习方法，说明在连续动作推荐环境中，仅依赖随机动作、离线行为拟合或即时反馈更新难以充分利用多轮交互信息。PPO-Rec 和 DDPG-Rec 均高于对应 SB3 对照，说明针对状态尺度、动作范围和短会话反馈特点进行训练设置调整后，策略更容易进入高反馈动作区域。该结果支持本文关于“面向推荐任务特点的强化学习策略实现能够改善通用 DRL 方法表现”的主要观察，但仍应结合前述训练预算和评估 episode 差异进行解释。

如@fig-final-ctr 所示，在当前实验设置下，PPO-Rec 和 DDPG-Rec 的平均 CTR 均高于传统基线和 SB3 对照。PPO-Rec 相比 PPO-SB3 的 CTR 绝对提升约为 0.4512，DDPG-Rec 相比 DDPG-SB3 的 CTR 绝对提升约为 0.5927。这一结果表明，在 VirtualTB-v0 这种状态维度较高、动作连续且 episode 较短的推荐环境中，直接使用通用强化学习实现并不一定能充分发挥算法潜力；面向推荐任务特点引入状态归一化、动作范围约束、稳定训练设置和基于环境指标的最优策略保存，对最终效果具有影响。

#capfig(
  image("figures/final_rebuild/final_ctr_mean_std.png", width: 80%),
  caption: [各算法最终 CTR 对比],
  label: <fig-final-ctr>,
)

@fig-final-ctr-seed 展示了不同随机种子下各方法的最终 CTR。可以看到，DDPG-Rec 不仅平均 CTR 最高，而且三个种子之间的波动较小，标准差仅为 0.0064。PPO-Rec 的平均 CTR 也较高，但 seed=2 的结果低于 seed=1 和 seed=3，因此稳定性略弱于 DDPG-Rec。DDPG-SB3 的 seed 间差异较大，说明通用 DDPG 实现在当前环境中对随机种子和训练过程较为敏感。

#capfig(
  image("figures/final_rebuild/final_ctr_by_seed_scatter.png", width: 80%),
  caption: [各随机种子下的最终 CTR 分布],
  label: <fig-final-ctr-seed>,
)

== 传统基线结果分析

传统基线的结果反映了不同非强化学习方法在 VirtualTB-v0 中的基本表现。Random 策略完全不利用状态信息，因此 CTR 接近零，可以视为无学习下界。MLP-SL 通过离线数据学习状态到动作的映射，CTR 明显高于 Random，说明离线行为数据中确实包含一定可学习模式。然而，MLP-SL 的目标是拟合离线动作，而不是直接优化环境中的点击反馈，因此其最终 CTR 仍然有限。

LinUCB 的表现高于 MLP-SL，说明在固定候选动作集合中利用上下文和即时 reward 进行选择，可以比单纯离线动作拟合获得更好的点击反馈。但 LinUCB 的动作选择受限于预先构造的动作原型，且不考虑动作对后续状态和长期累计收益的影响。因此，它适合作为上下文 Bandit 对照，而不能替代完整强化学习策略。

值得注意的是，MLP-SL 的动作范数明显低于其他方法，说明其输出动作相对保守。这可能与其监督学习目标有关：模型倾向于拟合离线动作分布，而不会主动探索环境中更高收益的动作区域。该现象也说明，在 VirtualTB-v0 中，仅依赖离线动作拟合难以充分利用环境反馈。

== 深度强化学习结果分析

@fig-drl-ctr-curve 展示了四种深度强化学习方法在训练过程中的 CTR 曲线。需要说明的是，训练曲线来自周期性环境评估，反映的是训练过程中当前策略的表现；而@tab-main-results 中的最终结果来自训练结束后对最优策略进行的正式评估。因此，曲线分析主要用于观察学习过程和收敛趋势，最终方法比较仍以正式评估结果为准。

#capfig(
  image("figures/final_rebuild/drl_ctr_curves_mean_std.png", width: 85%),
  caption: [深度强化学习方法训练 CTR 曲线],
  label: <fig-drl-ctr-curve>,
)

从训练曲线可以看出，PPO-SB3 在训练前期能够较快提升 CTR，但后续出现平台期和一定波动；DDPG-SB3 整体提升较慢，且不同 seed 之间差异明显。这说明通用强化学习实现虽然能够在 VirtualTB-v0 上学习，但在状态尺度、动作空间和短会话反馈方面并未充分体现连续动作推荐任务的特点。

PPO-Rec 在训练过程中 CTR 提升明显，最终三个随机种子的平均 CTR 达到 0.8670。与 PPO-SB3 相比，PPO-Rec 在状态归一化、动作范围约束、策略分布设置和确定性评估方面进行了面向推荐任务的设计，使其能够更稳定地找到高反馈动作区域。不过，PPO-Rec 仍存在一定 seed 差异，说明 on-policy 方法在当前环境中仍可能受到初始策略、采样轨迹和优势估计波动的影响。

DDPG-Rec 的训练速度和 seed 间稳定性在当前实验中较为突出。其最终 CTR 为 0.8958，三个随机种子结果非常接近。由于 DDPG 使用经验回放重复利用历史样本，且采用确定性策略直接输出连续动作，因此在 VirtualTB-v0 这种短会话、即时点击反馈较明确的环境中具有较高样本效率。但这一结果也需要谨慎解释：高 CTR 说明策略能够很好地适应当前固定模拟器，并不意味着该策略一定能直接泛化到真实线上环境。

== 动作范数与策略行为分析

动作范数用于辅助观察连续动作策略的输出幅度。@fig-action-norm-curve 展示了深度强化学习方法训练过程中的动作范数变化。可以看到，DDPG-SB3 的动作范数长期处于较高水平，但其 CTR 并不高，说明单纯输出更大幅度的动作并不等价于更好的推荐效果。DDPG-Rec 的动作范数同样较高，但其 CTR 和平均奖励也更高，说明其不仅输出较大动作，而且更可能学习到了对当前虚拟环境有效的动作方向。

#capfig(
  image("figures/final_rebuild/drl_action_norm_curves_mean_std.png", width: 85%),
  caption: [深度强化学习方法动作范数变化曲线],
  label: <fig-action-norm-curve>,
)

PPO-Rec 的动作范数低于 DDPG-Rec，但最终 CTR 仍然远高于 PPO-SB3。这说明推荐任务改进并不只是简单扩大动作幅度，而是通过状态归一化、稳定训练和合理评估，使策略更有效地利用连续动作空间。动作范数分析也提示，VirtualTB-v0 中较高的 CTR 可能与策略对固定模拟器中高响应动作区域的适应有关，因此后续工作可以进一步研究动作约束、动作方向正则和跨环境泛化评估。

== 学习速度与稳定性分析

@fig-learning-speed 展示了不同深度强化学习方法达到若干 CTR 阈值所需的训练步数。PPO-SB3 和 DDPG-SB3 在多数随机种子下未达到 CTR 0.7，而 PPO-Rec 和 DDPG-Rec 均能达到较高阈值。PPO-Rec 达到 CTR 0.7 的步数大致位于 22 万至 34 万步之间；DDPG-Rec 达到 CTR 0.7 的步数大致位于 7 万至 19.5 万步之间，并且在三个随机种子中都能达到接近 0.9 的周期评估 CTR。

#capfig(
  image("figures/final_rebuild/learning_speed_thresholds.png", width: 80%),
  caption: [不同方法达到 CTR 阈值的训练步数],
  label: <fig-learning-speed>,
)

这一结果说明，在当前 VirtualTB-v0 任务中，DDPG-Rec 的样本效率较高，能够在较少交互步数内获得较好策略。其原因可能包括：off-policy 经验回放能够重复利用历史交互样本；确定性策略更适合在连续动作空间中稳定输出高反馈动作；较小学习率、目标网络和梯度裁剪等设置降低了训练震荡。

不过，学习速度比较也存在需要说明的限制。不同方法的评估频率和最终评估 episode 数并不完全相同，因此阈值图主要用于观察训练趋势，而不能单独作为最终性能结论。本文最终结论仍以统一整理后的最终评估均值、标准差和多指标对比为主。

== 本章小结

本章对 Random、MLP-SL、LinUCB、PPO-SB3、DDPG-SB3、PPO-Rec 和 DDPG-Rec 七类方法进行了实验比较。结果表明，传统基线能够提供由无学习到即时反馈优化的参照，但在 VirtualTB-v0 连续动作推荐环境中效果有限；标准 SB3 强化学习方法能够学习有效策略，但最终 CTR 和稳定性均弱于面向推荐任务改进后的策略；PPO-Rec 和 DDPG-Rec 通过状态归一化、动作范围约束、稳定训练设置和基于环境指标的策略选择提高了最终 CTR 与平均奖励。其中，DDPG-Rec 在当前实验设置下取得最高平均 CTR，并表现出较低的 seed 间波动和较快的学习速度。需要强调的是，所有结论均限定在 VirtualTB-v0 固定模拟环境内，不能直接外推为真实电商平台中的线上效果。

= 总结与展望

== 本文工作总结

本文围绕虚拟电商推荐场景中的连续决策问题，开展了基于深度强化学习的推荐算法研究。与传统静态推荐任务不同，VirtualTB-v0 环境中的推荐策略需要在一个用户会话中连续输出动作，并根据虚拟用户反馈不断调整策略。因此，本文将推荐过程建模为马尔可夫决策过程，以用户和会话信息作为状态，以 27 维连续推荐向量作为动作，以点击反馈作为奖励，在此基础上比较不同推荐策略的表现。

首先，本文在原始 Virtual-Taobao 工作基础上构建了可运行的 VirtualTB-v0 强化学习环境。原始 Virtual-Taobao 工作基于真实匿名淘宝交互数据训练了用户生成、用户反馈和用户离开相关模型，本文没有重新训练这些模型，而是复用其发布的预训练权重，将其封装为符合 Gymnasium 规范的强化学习环境。通过该环境，推荐策略可以进行标准环境交互，并在统一状态空间、动作空间和奖励定义下训练与评估。

其次，本文设计了多层次对照方法。传统基线包括 Random、MLP-SL 和 LinUCB，分别表示无学习策略、离线监督学习策略和只优化即时反馈的上下文 Bandit 方法。深度强化学习方面，本文引入 PPO-SB3 和 DDPG-SB3 作为通用强化学习实现的对照，并进一步面向连续动作推荐任务的状态尺度、连续动作空间和短会话反馈特点，设计了 PPO-Rec 和 DDPG-Rec 两种推荐强化学习策略。

最后，本文完成了多随机种子的实验评估，并从最终 CTR、平均累计奖励、训练曲线、动作范数和稳定性等角度对实验结果进行分析。实验结果表明，在 VirtualTB-v0 固定模拟环境中，PPO-Rec 和 DDPG-Rec 的平均 CTR 均高于传统基线和标准 SB3 对照，其中 DDPG-Rec 在最终 CTR、seed 间稳定性和学习速度方面表现较为突出。



== 当前结论

根据第四章实验结果，可以得到以下结论。

第一，VirtualTB-v0 可以作为研究连续动作强化学习推荐策略的可控实验环境。该环境提供 91 维状态、27 维连续动作和基于点击反馈的 reward，使推荐过程能够被建模为标准强化学习交互过程。虽然该环境不能完全代表真实电商平台，但它为本文研究深度强化学习推荐策略提供了可复现的实验基础。

第二，传统基线方法在该环境中具有一定参考价值，但最终效果有限。Random 策略 CTR 极低，说明随机连续动作难以获得有效点击反馈；MLP-SL 能够通过离线数据学习到一定动作模式，但其训练目标是拟合离线动作，而不是直接优化环境 reward；LinUCB 能够利用上下文和即时反馈提升效果，但受限于固定动作原型和单步优化目标，整体性能仍低于深度强化学习方法。

第三，通用深度强化学习实现能够在 VirtualTB-v0 中学习有效策略，但直接使用时效果并不充分。PPO-SB3 和 DDPG-SB3 的最终 CTR 均高于传统基线，说明强化学习交互训练具有价值；但二者与 PPO-Rec、DDPG-Rec 相比存在明显差距，说明在当前连续动作推荐任务中，状态尺度处理、动作输出约束、训练稳定性设置和基于环境指标的策略选择都会影响最终结果。

第四，面向推荐任务改进的强化学习策略在当前实验设置下取得了更高的平均 CTR。PPO-Rec 的最终 CTR 为 0.8670 ± 0.0529，DDPG-Rec 的最终 CTR 为 0.8958 ± 0.0064，均高于 SB3 对照和传统基线。其中，DDPG-Rec 训练 300k 步便取得最高最终 CTR，说明 off-policy 经验回放、确定性策略输出以及稳定训练设置在当前短会话连续动作环境中具有较高样本效率。

第五，动作范数分析表明，高 CTR 并不能简单归因于动作幅度变大。DDPG-SB3 的动作范数较高但 CTR 不高，而 DDPG-Rec 在较高动作范数下同时获得较高 CTR 和奖励，说明策略是否学习到有效动作方向比动作幅度本身更重要。动作范数可以作为连续动作策略行为分析的辅助指标，但不能单独作为推荐效果指标。



== 不足与展望

本文仍存在一些不足。

首先，实验结论依赖于 VirtualTB-v0 固定模拟环境。虽然原始 Virtual-Taobao 模拟器来源于真实匿名交互数据训练得到的虚拟环境，但本文实验并未在真实线上系统中验证，也没有在多个不同模拟器或不同用户分布上测试策略泛化能力。因此，本文结果只能说明算法在当前 VirtualTB-v0 环境中的表现，不能直接外推为真实电商平台中的线上效果。

其次，当前状态表示仍较为有限。VirtualTB-v0 提供的是 91 维状态向量，其中包含用户静态特征、上一轮反馈和当前页码，但没有显式提供商品编号、候选商品集合、完整点击序列或用户长期兴趣变化。因此，本文没有引入基于 Embedding、RNN 或 Transformer 的序列兴趣建模方法。未来如果使用更丰富的推荐环境，可以进一步研究用户动态兴趣表示和序列推荐策略。

再次，本文的奖励主要来自点击反馈。点击是电商推荐中的重要指标，但真实推荐系统通常还需要同时考虑购买、停留时长、跳出率、多样性和长期留存等目标。仅以点击作为 reward 可能导致策略偏向短期点击收益。未来可以在更丰富的环境中构造多目标奖励函数，研究强化学习推荐策略在点击收益和用户长期体验之间的平衡。

此外，PPO-Rec 和 DDPG-Rec 在 VirtualTB-v0 中取得较高 CTR，但这也提示策略可能较强地适应了固定模拟器中的高响应区域。未来工作可以考虑引入动作范数约束、保守策略更新、离线强化学习或跨模拟器验证，以降低策略对单一环境的过度适应风险。

最后，本文主要关注算法复现、推荐任务建模和实验比较，尚未进一步研究更复杂的推荐系统工程问题。例如，真实推荐系统通常包含召回、排序、重排和业务约束等多个环节，而 VirtualTB-v0 使用连续动作向量抽象推荐动作，不能完全覆盖真实推荐链路。后续研究可以在更贴近真实推荐流程的环境中扩展本文方法，并进一步分析强化学习策略与传统推荐模型之间的结合方式。
